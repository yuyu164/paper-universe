# 《镂影》剪纸小游戏设计文档 v2

> **设计原则**：所有操作均为点击/选择，零绘制、零拖拽、零精细操作。操作简单，但决策有深度。

---

## 游戏名称：《镂影》

> **核心概念**：选择折叠方式，选择剪裁位置，展开揭晓。你的每一次选择都决定最终图案。

---

## 一、原版问题诊断

| 问题 | 原因 | 影响 |
|------|------|------|
| 自由绘制刀路 | 鼠标/手指无法精确控制曲线 | 操作极其难受 |
| 折叠线自由画 | 同上 | 折叠位置不精确 |
| 展开计算复杂 | 自由路径的对称计算量大 | 性能差、bug 多 |
| 上手门槛高 | 需要理解折叠+刀路+展开的完整概念 | 新玩家劝退 |

**v2 核心改变**：**从"自由创作工具"变为"策略选择游戏"**。

---

## 二、核心玩法：选择，而非绘制

### 2.1 一句话描述

> 玩家面对一张红纸，通过**选择预设的折叠方式**和**选择预设的剪裁形状**，组合出目标图案。

### 2.2 操作流程（全部点击）

```
Step 1: 看目标 → 系统展示目标纹样
Step 2: 选折叠 → 从 2-4 个预设折叠方式中点击选择一个
Step 3: 选剪裁 → 从 3-6 个预设剪裁形状中点击选择一个
Step 4: 看结果 → 自动展开，与目标对比，评分
Step 5: 重试或下一关
```

**全程只有点击，没有任何拖拽/绘制/滑动。**

### 2.3 为什么这样设计仍然有游戏性

| 策略点 | 说明 |
|--------|------|
| **折叠选择** | 同一个剪裁形状，不同折叠方式展开后完全不同。横向折叠剪一个三角 → 展开是菱形；对角折叠剪同一个三角 → 展开是完全不同的图案。 |
| **剪裁选择** | 同一个折叠方式，不同剪裁位置/形状产生不同结果。剪掉角 vs 剪掉边 vs 剪掉中心，展开后天差地别。 |
| **组合探索** | 3 种折叠 × 4 种剪裁 = 12 种组合，但只有 1-2 种能匹配目标。玩家需要**逆向推理**：目标图案需要什么对称性？什么剪裁能产生这种形状？ |
| **关卡递进** | 前期折叠少、选项少（2×3=6 种组合，蒙也能蒙对）；后期折叠多、选项多、需要多步操作（组合爆炸，必须思考）。 |

---

## 三、详细机制设计

### 3.1 折叠系统

**折叠不是自由画线，而是从预设选项中选择。**

每个关卡提供 2-4 个折叠选项，每个选项是一个预设的折叠方式：

| 折叠选项 | 视觉表现 | 展开效果 |
|---------|---------|---------|
| **横向对折** | 纸张上下合拢，显示"×2" | 左右镜像对称 |
| **纵向对折** | 纸张左右合拢，显示"×2" | 上下镜像对称 |
| **对角折叠** | 纸张沿左上-右下对角合拢 | 对角线对称 |
| **三角折叠** | 纸张折成三角形（三等分） | 三重旋转对称 |
| **四折** | 纸张折成四分之一 | 四重对称 |
| **八折** | 纸张折成八分之一 | 八重对称（雪花类图案） |

**选择折叠时的视觉反馈**：
- 点击某个折叠选项 → 纸张播放折叠动画（CSS 3D transform，0.5秒）
- 折叠后纸张缩小到对应区域，显示折叠层数

### 3.2 剪裁系统

**剪裁不是自由画线，而是从预设的剪裁形状中选择位置。**

每个关卡提供 3-6 个剪裁选项，每个选项是在折叠后纸张上的一个预设剪裁位置：

| 剪裁选项 | 视觉表现（在折叠后的纸上） | 典型展开效果 |
|---------|------------------------|-------------|
| **剪掉右上角** | 右上角一个三角形被标红 | 展开后四个角都被剪掉 |
| **剪掉底边** | 底边一段弧线被标红 | 展开后边缘出现波浪/弧形 |
| **剪掉中心圆** | 中心一个圆形被标红 | 展开后中心出现对称镂空 |
| **剪掉半边** | 纸张右半部分被标红 | 展开后只剩左半的镜像 |
| **剪出 V 形** | 边缘一个 V 形缺口 | 展开后出现锯齿/花瓣边缘 |
| **剪出弧形** | 边缘一个弧形缺口 | 展开后出现圆润的花瓣 |

**选择剪裁时的视觉反馈**：
- 鼠标 hover 某个选项 → 折叠后的纸张上对应区域高亮闪烁（红色半透明）
- 点击确认 → 该区域播放"剪掉"动画（渐隐 + 纸屑飘落）

### 3.3 多步操作（进阶关卡）

前期关卡：1 次折叠 + 1 次剪裁 = 展开
后期关卡：1 次折叠 + 1 次剪裁 → **再折叠** + **再剪裁** → 展开

```
简单关卡：  折叠 → 剪裁 → 展开
中等关卡：  折叠 → 剪裁 → 折叠 → 剪裁 → 展开
困难关卡：  折叠 → 剪裁 → 折叠 → 剪裁 → 折叠 → 剪裁 → 展开
```

**多步操作的策略深度**：
- 第一步的剪裁决定了第二步的纸张形状
- 玩家需要预判"剪完这一刀之后，再折叠会变成什么样"
- 这就是游戏性的核心：**链式推理**

### 3.4 展开与评分

**展开**：点击"展开"按钮（或自动展开），纸张按折叠逆序逐层展开（CSS 动画）。

**评分**：
- 展开后，玩家作品与目标图案自动对比
- 使用像素级对比计算重合度（IoU）
- 评分维度：

| 维度 | 权重 | 说明 |
|------|------|------|
| 完成度 | 70% | 与目标图案的形状重合度 |
| 步骤效率 | 30% | 使用的操作步数越少越好 |

**星级**：
- ★★★★★：完成度 ≥ 95%
- ★★★★☆：完成度 ≥ 85%
- ★★★☆☆：完成度 ≥ 70%
- ★★☆☆☆：完成度 ≥ 50%
- ★☆☆☆☆：完成度 < 50%（过关但表现差）

---

## 四、关卡设计

### 4.1 关卡结构

每关定义：
- **目标图案**：一个 SVG 纹样
- **折叠选项**：2-4 个预设折叠方式
- **剪裁选项**：3-6 个预设剪裁位置
- **操作步数**：1-3 步（折叠+剪裁算一步）
- **正确解法**：1-2 种（但玩家不知道，需要自己探索）

### 4.2 关卡示例

#### 第 1 关：窗花（教学关）

```
目标：一个简单的四瓣花

折叠选项（2选1）：
  A. 横向对折    B. 纵向对折

剪裁选项（3选1）：
  A. 剪掉右上角（三角形）
  B. 剪掉中心（小圆）
  C. 剪掉底边（弧线）

正确答案：A + A（横向对折 + 剪掉右上角 → 展开成四瓣花）
```

#### 第 5 关：雪花

```
目标：六角雪花

折叠选项（3选1）：
  A. 三角折叠（三等分）
  B. 四折
  C. 对角折叠

剪裁选项（4选1）：
  A. 剪掉顶角（三角形）
  B. 剪掉两侧（两个小三角）
  C. 剪出边缘 V 形
  D. 剪掉中心

正确答案：A + C（三角折叠 + 边缘 V 形 → 六角雪花）
```

#### 第 12 关：蝴蝶

```
目标：蝴蝶图案

Step 1 折叠选项（2选1）：
  A. 纵向对折    B. 横向对折

Step 1 剪裁选项（3选1）：
  A. 剪掉上边（大弧形）
  B. 剪掉中心（椭圆）
  C. 剪出两侧 V 形

Step 2 折叠选项（2选1）：
  A. 对角折叠    B. 不再折叠

Step 2 剪裁选项（3选1）：
  A. 剪掉下角
  B. 剪出触须（顶部两个小三角）
  C. 剪掉中心小圆

正确答案：A + A → A + B（纵向对折 + 上边弧形 → 对角折叠 + 触须三角 → 蝴蝶）
```

### 4.3 关卡总览（30 关）

| 章节 | 关卡 | 主题 | 操作步数 | 选项数量 | 难度 |
|------|------|------|---------|---------|------|
| **第一章：入门** | 1-5 | 基础对称 | 1步 | 2×3 | ★☆☆☆☆ |
| **第二章：进阶** | 6-10 | 多种折叠 | 1步 | 3×4 | ★★☆☆☆ |
| **第三章：链式** | 11-15 | 两步操作 | 2步 | 2×3 → 2×3 | ★★★☆☆ |
| **第四章：复杂** | 16-20 | 多步+多选 | 2-3步 | 3×4 → 3×4 | ★★★★☆ |
| **第五章：大师** | 21-25 | 高级纹样 | 3步 | 3×5 → 3×5 | ★★★★★ |
| **第六章：挑战** | 26-30 | 极限优化 | 2-3步 | 4×6 | ★★★★★ |

---

## 五、游戏模式

### 5.1 闯关模式（主要模式）

- 30 关，按章节递进
- 每关有 1-3 个正确解法
- 通关解锁下一关
- 每关可重复挑战，追求更高评分

### 5.2 挑战模式

- 给定目标纹样，提供更多折叠和剪裁选项
- 目标：用最少步数完成
- 适合通关后反复游玩

### 5.3 自由模式

- 无目标图案，自由选择折叠和剪裁
- 纯粹享受"展开瞬间"的惊喜感
- 可保存喜欢的作品

---

## 六、界面设计

### 6.1 游戏主界面

```
┌─────────────────────────────────────────────────────────┐
│  ← 返回     纹样复原 · 第 3 关     ★★☆☆☆     重置 ↻   │
├──────────────────────┬──────────────────────────────────┤
│                      │                                  │
│   ┌──────────────┐   │      ┌──────────────────┐        │
│   │              │   │      │                  │        │
│   │  目标纹样     │   │      │    纸张操作区     │        │
│   │  (白色图案    │   │      │    (红色方形)     │        │
│   │   红色背景)   │   │      │                  │        │
│   │              │   │      │  显示当前折叠状态   │        │
│   └──────────────┘   │      │  和已剪裁区域     │        │
│                      │      └──────────────────┘        │
│   完成度：——          │                                  │
│   步骤：1/2           │                                  │
│                      │                                  │
├──────────────────────┴──────────────────────────────────┤
│                                                         │
│  ┌─ 选择折叠方式 ─────────────────────────────────────┐ │
│  │  [横向对折]  [纵向对折]  [对角折叠]                 │ │
│  └────────────────────────────────────────────────────┘ │
│                                                         │
│  ┌─ 选择剪裁位置 ─────────────────────────────────────┐ │
│  │  [剪角△]  [剪边⌒]  [剪中心○]  [剪半□]            │ │
│  └────────────────────────────────────────────────────┘ │
│                                                         │
│              [ 展开 → ]                                  │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 6.2 移动端布局

```
┌──────────────────────┐
│  ← 第 3 关    ↻ 重置 │
├──────────────────────┤
│                      │
│   ┌──────────────┐   │
│   │  目标纹样     │   │
│   └──────────────┘   │
│                      │
│   ┌──────────────┐   │
│   │              │   │
│   │  纸张操作区   │   │
│   │              │   │
│   └──────────────┘   │
│                      │
│  选择折叠：           │
│  [横折] [纵折] [对角] │
│                      │
│  选择剪裁：           │
│  [△] [⌒] [○] [□]    │
│                      │
│    [ 展开 → ]        │
└──────────────────────┘
```

### 6.3 交互细节

**选择折叠时**：
- 点击折叠按钮 → 纸张播放折叠动画（0.5s）
- 已选折叠按钮高亮（`bg-paper-500 text-white`）
- 折叠后，剪裁选项自动更新（根据折叠后的纸张形状）

**选择剪裁时**：
- hover 剪裁按钮 → 纸张上对应区域闪烁预览（红色半透明覆盖）
- 点击确认 → 该区域播放剪掉动画（0.3s）+ 纸屑效果
- 剪裁后，如果还有操作步数，折叠选项更新

**展开时**：
- 点击"展开" → 纸张逐层展开动画（每层 0.4s）
- 展开完成 → 目标图案叠加对比（重合部分绿色，差异部分红色）
- 0.5s 后显示评分和星级

---

## 七、视觉风格

### 7.1 配色

| 元素 | 颜色 | Tailwind |
|------|------|---------|
| 游戏背景 | 深红 | `bg-paper-950` |
| 纸张 | 正红 | `bg-paper-500` |
| 镂空区域 | 透明（显示背景） | `bg-transparent` |
| 目标图案 | 白色 | `text-white` / `fill-white` |
| 按钮-选中 | 正红底白字 | `bg-paper-500 text-white` |
| 按钮-未选中 | 白底深色字 | `bg-white text-ink-900` |
| 按钮-hover | 浅红底 | `hover:bg-paper-100` |
| 高亮预览 | 红色半透明 | `bg-paper-500/30` |

### 7.2 纸张质感

- 纸张使用微妙的 CSS 渐变模拟宣纸纹理
- 折叠时纸张有轻微阴影变化
- 剪裁掉落区域有渐隐动画

### 7.3 纸屑效果

剪裁确认后，从剪裁位置飘出 5-8 个红色小三角形纸屑：
```css
@keyframes paper-scrap {
  0% { transform: translate(0, 0) rotate(0deg); opacity: 1; }
  100% { transform: translate(var(--dx), var(--dy)) rotate(var(--rot)); opacity: 0; }
}
```
- 使用 CSS 自定义属性 `--dx`、`--dy`、`--rot` 控制每个纸屑的方向
- 纯 CSS 动画，零 JS 计算

---

## 八、技术实现要点

### 8.1 核心数据结构

```typescript
// 折叠选项
interface FoldOption {
  id: string
  name: string              // "横向对折"
  icon: string              // SVG 图标
  type: FoldType
  // 折叠后的裁剪区域坐标（预计算好的）
  cutRegions: CutRegion[]
}

// 剪裁选项
interface CutOption {
  id: string
  name: string              // "剪掉右上角"
  icon: string              // SVG 图标
  // 这个剪裁在折叠后纸张上的区域（预计算的 SVG path）
  region: string            // SVG path d 属性
  // 展开后的完整镂空区域（预计算的 SVG path）
  unfoldedRegion: string    // SVG path d 属性
}

// 关卡
interface Level {
  id: string
  targetPattern: string     // 目标纹样 SVG
  steps: LevelStep[]        // 每步的选项
}

interface LevelStep {
  foldOptions: FoldOption[]
  cutOptions: CutOption[]
}
```

### 8.2 关键简化

**所有展开结果都是预计算的**。

每个 `CutOption` 的 `unfoldedRegion` 字段存储了该剪裁在当前折叠方式下展开后的完整 SVG path。这意味着：

- **不需要实时计算展开** → 零性能问题
- **不需要几何引擎** → 零 bug 风险
- **只需要叠加 SVG** → 渲染极简

```typescript
// 展开后的结果 = 所有步骤的 unfoldedRegion 叠加
function getResultPattern(steps: SelectedStep[]): string {
  return steps
    .map(step => step.selectedCut.unfoldedRegion)
    .join(' ')
}
```

### 8.3 评分计算

```typescript
// 使用 Canvas 像素对比
function calculateScore(targetSVG: string, resultSVG: string): number {
  // 1. 将两个 SVG 分别绘制到离屏 Canvas
  // 2. 获取像素数据
  // 3. 计算 IoU（交并比）
  // 4. 返回 0-100 的分数
}
```

### 8.4 动画实现

| 动画 | 实现 | 时长 |
|------|------|------|
| 折叠 | CSS `transform: rotateY() scale()` + `transition` | 500ms |
| 剪裁 | CSS `opacity: 0` + `transform: scale(0.8)` | 300ms |
| 纸屑 | CSS `@keyframes` + 自定义属性 | 800ms |
| 展开 | CSS `transform: rotateY() scale()` 反向 | 400ms/层 |
| 评分 | CSS `@keyframes fade-up` | 600ms |

---

## 九、与原版对比

| 维度 | v1（绘制式） | v2（选择式） |
|------|-------------|-------------|
| **操作方式** | 自由绘制刀路/折叠线 | 点击预设选项 |
| **操作精度要求** | 极高（鼠标精确控制） | 零（点击即可） |
| **移动端体验** | 极差（手指遮挡+滑动不精确） | 极好（点击按钮） |
| **策略深度** | 高但被操作门槛掩盖 | 高且可触及（思考而非操作） |
| **实现复杂度** | 极高（几何引擎+实时计算） | 低（预计算+SVG叠加） |
| **bug 风险** | 高（浮点精度+边界情况） | 低（所有数据预计算） |
| **性能** | 差（实时几何计算） | 优（纯 CSS 动画+SVG） |
| **可玩性** | 操作劝退，难以体验 | 上手即玩，思考有深度 |

---

## 十、开发优先级

### P0（核心可玩）

- 折叠选择 UI + 动画
- 剪裁选择 UI + 动画
- 展开动画
- 评分系统
- 前 10 关数据

### P1（完整体验）

- 多步操作（2-3 步关卡）
- 全部 30 关数据
- 挑战模式
- 关卡选择界面

### P2（锦上添花）

- 自由模式
- 纸屑粒子效果
- 音效
- 移动端手势优化

---

## 十一、开发执行顺序

```
Step 1:  定义游戏类型（types/game.ts）
Step 2:  实现关卡数据结构（game/data/levels.ts）
Step 3:  实现前 5 关的预计算数据
Step 4:  实现 PaperCanvas 组件（SVG 渲染纸张+折叠+剪裁状态）
Step 5:  实现折叠选择 UI（FoldSelector 组件）
Step 6:  实现剪裁选择 UI（CutSelector 组件）
Step 7:  实现展开动画
Step 8:  实现评分计算（Canvas 像素对比）
Step 9:  实现 GameContainer（状态管理+组装）
Step 10: 实现 LevelSelect（关卡选择）
Step 11: 替换 InteractiveSection
Step 12: 测试 + 修复
```

---

**设计理念**：

> v1 的失败在于把"剪纸的工具"当成了"剪纸的游戏"。真正的游戏不需要你精确地控制剪刀——它只需要你做出正确的**选择**。折叠方式决定了对称性，剪裁位置决定了形状，而你需要逆向推理：什么样的组合能产生目标图案？这就是《镂影》v2 的核心乐趣。
