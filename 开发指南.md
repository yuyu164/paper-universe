# 《镂影》剪纸小游戏开发指南

> **目标读者**：AI 开发执行者
> **上游文档**：《剪纸小游戏设计.md》
> **技术约束**：Next.js + Tailwind CSS + TypeScript，纯前端，无后端

---

## 一、项目结构

```
src/
├── app/
│   └── page.tsx                    # 主页面（保持原有网站结构）
│
├── components/
│   ├── sections/
│   │   └── InteractiveSection.tsx  # 替换原有互动区，作为游戏入口
│   │
│   └── game/                       # 游戏专用组件
│       ├── GameContainer.tsx       # 游戏主容器（状态管理）
│       ├── PaperCanvas.tsx         # 纸张操作区（折叠+剪裁）
│       ├── TargetPreview.tsx       # 目标纹样预览
│       ├── Toolbar.tsx             # 工具栏（折叠/剪裁/展开/提示）
│       ├── ScoreDisplay.tsx        # 评分展示
│       ├── LevelSelect.tsx         # 关卡选择
│       └── ModeSelect.tsx          # 模式选择（闯关/挑战/自由）
│
├── game/                           # 游戏核心逻辑
│   ├── engine/
│   │   ├── fold.ts                 # 折叠计算
│   │   ├── cut.ts                  # 剪裁计算
│   │   ├── unfold.ts               # 展开计算
│   │   └── score.ts                # 评分计算
│   │
│   ├── data/
│   │   ├── levels.ts               # 关卡数据
│   │   ├── patterns.ts             # 目标纹样 SVG 数据
│   │   └── techniques.ts           # 技法定义
│   │
│   └── hooks/
│       ├── useGameState.ts         # 游戏状态管理
│       ├── usePaper.ts             # 纸张操作状态
│       └── useScore.ts             # 评分逻辑
│
├── types/
│   └── game.ts                     # 游戏相关类型定义
│
└── lib/
    └── geometry.ts                 # 几何计算工具函数
```

---

## 二、类型定义

**文件**：`src/types/game.ts`

```typescript
// === 基础类型 ===

export interface Point {
  x: number
  y: number
}

export interface Line {
  start: Point
  end: Point
}

export interface Path {
  points: Point[]
  closed: boolean
}

// === 折叠相关 ===

export type FoldDirection = 'horizontal' | 'vertical' | 'diagonal-left' | 'diagonal-right'

export interface Fold {
  id: string
  direction: FoldDirection
  line: Line              // 折叠线
  timestamp: number
}

// === 剪裁相关 ===

export interface Cut {
  id: string
  path: Path              // 刀路路径
  technique: TechniqueType
  timestamp: number
}

export type TechniqueType = 'basic' | 'crescent' | 'zigzag' | 'hollow' | 'breakthrough'

// === 纸张状态 ===

export interface PaperState {
  width: number           // 纸张尺寸（统一为 100）
  height: number
  folds: Fold[]           // 折叠历史
  cuts: Cut[]             // 剪裁历史
  currentLayer: number    // 当前层数（1/2/4/8）
  cutoutAreas: Path[]     // 已镂空区域
}

// === 关卡相关 ===

export interface Level {
  id: string
  chapter: number
  order: number
  name: string
  description: string
  targetPattern: string   // 目标纹样 SVG path
  targetImage: string     // 目标纹样预览图路径
  constraints: {
    maxFolds: number
    maxCuts: number
    availableTechniques: TechniqueType[]
  }
  difficulty: 1 | 2 | 3 | 4 | 5
  hints: string[]         // 提示文本（3级）
}

// === 游戏状态 ===

export type GameMode = 'level' | 'challenge' | 'free'

export interface GameState {
  mode: GameMode
  currentLevel: Level | null
  paperState: PaperState
  score: number
  isUnfolded: boolean
  isCompleted: boolean
  tool: 'fold' | 'cut' | null
  selectedFoldDirection: FoldDirection | null
  selectedTechnique: TechniqueType
}

// === 技法 ===

export interface Technique {
  type: TechniqueType
  name: string
  description: string
  unlockLevel: number
  icon: string            // SVG icon path
}
```

---

## 三、核心算法实现

### 3.1 几何计算工具

**文件**：`src/lib/geometry.ts`

```typescript
import { Point, Line, Path } from '@/types/game'

/**
 * 计算点关于直线的对称点
 */
export function reflectPoint(point: Point, line: Line): Point {
  const { start, end } = line
  const dx = end.x - start.x
  const dy = end.y - start.y
  const a = (dx * dx - dy * dy) / (dx * dx + dy * dy)
  const b = 2 * dx * dy / (dx * dx + dy * dy)
  
  return {
    x: a * (point.x - start.x) + b * (point.y - start.y) + start.x,
    y: b * (point.x - start.x) - a * (point.y - start.y) + start.y
  }
}

/**
 * 计算路径关于直线的对称路径
 */
export function reflectPath(path: Path, line: Line): Path {
  return {
    points: path.points.map(p => reflectPoint(p, line)),
    closed: path.closed
  }
}

/**
 * 判断点是否在路径内部（射线法）
 */
export function isPointInPath(point: Point, path: Path): boolean {
  let inside = false
  const n = path.points.length
  
  for (let i = 0, j = n - 1; i < n; j = i++) {
    const xi = path.points[i].x, yi = path.points[i].y
    const xj = path.points[j].x, yj = path.points[j].y
    
    if (((yi > point.y) !== (yj > point.y)) &&
        (point.x < (xj - xi) * (point.y - yi) / (yj - yi) + xi)) {
      inside = !inside
    }
  }
  
  return inside
}

/**
 * 计算两个路径的交集面积（用于评分）
 * 使用栅格化方法近似计算
 */
export function calculateIntersection(path1: Path, path2: Path, resolution: number = 100): number {
  let intersection = 0
  let union = 0
  
  for (let x = 0; x < resolution; x++) {
    for (let y = 0; y < resolution; y++) {
      const point = { x: x / resolution * 100, y: y / resolution * 100 }
      const in1 = isPointInPath(point, path1)
      const in2 = isPointInPath(point, path2)
      
      if (in1 && in2) intersection++
      if (in1 || in2) union++
    }
  }
  
  return union > 0 ? intersection / union : 0
}
```

### 3.2 折叠计算

**文件**：`src/game/engine/fold.ts`

```typescript
import { Fold, FoldDirection, Line, PaperState } from '@/types/game'

/**
 * 根据折叠方向生成折叠线
 */
export function createFoldLine(direction: FoldDirection, paperSize: number): Line {
  const center = paperSize / 2
  
  switch (direction) {
    case 'horizontal':
      return { start: { x: 0, y: center }, end: { x: paperSize, y: center } }
    case 'vertical':
      return { start: { x: center, y: 0 }, end: { x: center, y: paperSize } }
    case 'diagonal-left':
      return { start: { x: 0, y: 0 }, end: { x: paperSize, y: paperSize } }
    case 'diagonal-right':
      return { start: { x: paperSize, y: 0 }, end: { x: 0, y: paperSize } }
  }
}

/**
 * 执行折叠操作
 */
export function applyFold(state: PaperState, direction: FoldDirection): PaperState {
  const foldLine = createFoldLine(direction, state.width)
  
  const newFold: Fold = {
    id: `fold-${Date.now()}`,
    direction,
    line: foldLine,
    timestamp: Date.now()
  }
  
  return {
    ...state,
    folds: [...state.folds, newFold],
    currentLayer: state.currentLayer * 2,
    // 镂空区域也需要对称变换（简化处理，实际需要更复杂的计算）
    cutoutAreas: state.cutoutAreas.map(area => ({
      ...area,
      points: area.points.map(p => ({
        x: p.x,  // 简化：实际应根据折叠线计算
        y: p.y
      }))
    }))
  }
}

/**
 * 撤销最后一次折叠
 */
export function undoFold(state: PaperState): PaperState {
  if (state.folds.length === 0) return state
  
  const newFolds = state.folds.slice(0, -1)
  
  return {
    ...state,
    folds: newFolds,
    currentLayer: state.currentLayer / 2
  }
}
```

### 3.3 剪裁计算

**文件**：`src/game/engine/cut.ts`

```typescript
import { Cut, Path, PaperState, TechniqueType } from '@/types/game'

/**
 * 执行剪裁操作
 */
export function applyCut(state: PaperState, path: Path, technique: TechniqueType): PaperState {
  const newCut: Cut = {
    id: `cut-${Date.now()}`,
    path,
    technique,
    timestamp: Date.now()
  }
  
  // 根据技法处理路径
  let processedPath = path
  switch (technique) {
    case 'crescent':
      processedPath = applyCrescentTechnique(path)
      break
    case 'zigzag':
      processedPath = applyZigzagTechnique(path)
      break
    // ... 其他技法
  }
  
  return {
    ...state,
    cuts: [...state.cuts, newCut],
    cutoutAreas: [...state.cutoutAreas, processedPath]
  }
}

/**
 * 月牙剪技法：将直线转换为月牙形
 */
function applyCrescentTechnique(path: Path): Path {
  // 简化实现：在路径两侧添加弧形
  // 实际需要更复杂的曲线计算
  return path
}

/**
 * 锯齿剪技法：在路径边缘添加锯齿
 */
function applyZigzagTechnique(path: Path): Path {
  // 简化实现：在路径点之间插入锯齿点
  return path
}
```

### 3.4 展开计算

**文件**：`src/game/engine/unfold.ts`

```typescript
import { PaperState, Path } from '@/types/game'
import { reflectPath } from '@/lib/geometry'

/**
 * 展开纸张，计算最终图案
 */
export function unfoldPaper(state: PaperState): Path[] {
  let result = [...state.cutoutAreas]
  
  // 逆序应用折叠变换
  for (let i = state.folds.length - 1; i >= 0; i--) {
    const fold = state.folds[i]
    result = result.flatMap(path => [
      path,
      reflectPath(path, fold.line)
    ])
  }
  
  return result
}

/**
 * 生成最终展开图案的 SVG path 字符串
 */
export function generateUnfoldedSVG(state: PaperState): string {
  const unfoldedPaths = unfoldPaper(state)
  
  return unfoldedPaths.map(path => {
    const d = path.points.map((p, i) => 
      `${i === 0 ? 'M' : 'L'} ${p.x} ${p.y}`
    ).join(' ')
    
    return `<path d="${d}${path.closed ? ' Z' : ''}" fill="currentColor" />`
  }).join('\n')
}
```

### 3.5 评分计算

**文件**：`src/game/engine/score.ts`

```typescript
import { Level, PaperState } from '@/types/game'
import { unfoldPaper } from './unfold'
import { calculateIntersection, pathFromSVG } from '@/lib/geometry'

/**
 * 计算玩家作品的评分
 */
export function calculateScore(level: Level, state: PaperState): {
  score: number
  accuracy: number
  efficiency: number
  stars: number
} {
  // 1. 计算完成度（与目标图案的重合度）
  const targetPath = pathFromSVG(level.targetPattern)
  const resultPaths = unfoldPaper(state)
  
  // 简化：使用第一个镂空区域与目标比较
  // 实际应该合并所有镂空区域后比较
  const accuracy = resultPaths.length > 0
    ? calculateIntersection(targetPath, resultPaths[0])
    : 0
  
  // 2. 计算效率（剩余刀数 + 折叠简洁度）
  const usedCuts = state.cuts.length
  const usedFolds = state.folds.length
  const maxCuts = level.constraints.maxCuts
  const maxFolds = level.constraints.maxFolds
  
  const cutEfficiency = 1 - (usedCuts / maxCuts) * 0.5
  const foldEfficiency = 1 - (usedFolds / maxFolds) * 0.3
  const efficiency = (cutEfficiency + foldEfficiency) / 2
  
  // 3. 综合评分
  const score = accuracy * 0.7 + efficiency * 0.3
  
  // 4. 星级评定
  let stars = 1
  if (accuracy >= 0.95 && efficiency >= 0.8) stars = 5
  else if (accuracy >= 0.90 && efficiency >= 0.7) stars = 4
  else if (accuracy >= 0.80 && efficiency >= 0.6) stars = 3
  else if (accuracy >= 0.70) stars = 2
  
  return {
    score: Math.round(score * 100),
    accuracy: Math.round(accuracy * 100),
    efficiency: Math.round(efficiency * 100),
    stars
  }
}
```

---

## 四、游戏状态管理

**文件**：`src/game/hooks/useGameState.ts`

```typescript
import { useState, useCallback } from 'react'
import { GameState, GameMode, Level, FoldDirection, TechniqueType, Path } from '@/types/game'
import { applyFold, undoFold } from '@/game/engine/fold'
import { applyCut } from '@/game/engine/cut'
import { calculateScore } from '@/game/engine/score'

const initialState: GameState = {
  mode: 'level',
  currentLevel: null,
  paperState: {
    width: 100,
    height: 100,
    folds: [],
    cuts: [],
    currentLayer: 1,
    cutoutAreas: []
  },
  score: 0,
  isUnfolded: false,
  isCompleted: false,
  tool: null,
  selectedFoldDirection: null,
  selectedTechnique: 'basic'
}

export function useGameState() {
  const [state, setState] = useState<GameState>(initialState)
  
  // 开始关卡
  const startLevel = useCallback((level: Level) => {
    setState({
      ...initialState,
      mode: 'level',
      currentLevel: level
    })
  }, [])
  
  // 选择工具
  const selectTool = useCallback((tool: 'fold' | 'cut' | null) => {
    setState(prev => ({ ...prev, tool }))
  }, [])
  
  // 选择折叠方向
  const selectFoldDirection = useCallback((direction: FoldDirection) => {
    setState(prev => ({ ...prev, selectedFoldDirection: direction }))
  }, [])
  
  // 执行折叠
  const doFold = useCallback((direction: FoldDirection) => {
    setState(prev => {
      if (!prev.currentLevel) return prev
      if (prev.paperState.folds.length >= prev.currentLevel.constraints.maxFolds) {
        return prev  // 达到折叠上限
      }
      
      return {
        ...prev,
        paperState: applyFold(prev.paperState, direction)
      }
    })
  }, [])
  
  // 执行剪裁
  const doCut = useCallback((path: Path) => {
    setState(prev => {
      if (!prev.currentLevel) return prev
      if (prev.paperState.cuts.length >= prev.currentLevel.constraints.maxCuts) {
        return prev  // 达到刀数上限
      }
      
      return {
        ...prev,
        paperState: applyCut(prev.paperState, path, prev.selectedTechnique)
      }
    })
  }, [])
  
  // 展开验证
  const unfold = useCallback(() => {
    setState(prev => {
      if (!prev.currentLevel) return prev
      
      const result = calculateScore(prev.currentLevel, prev.paperState)
      
      return {
        ...prev,
        isUnfolded: true,
        score: result.score,
        isCompleted: result.stars >= 1
      }
    })
  }, [])
  
  // 重置
  const reset = useCallback(() => {
    setState(prev => ({
      ...initialState,
      mode: prev.mode,
      currentLevel: prev.currentLevel
    }))
  }, [])
  
  // 撤销
  const undo = useCallback(() => {
    setState(prev => ({
      ...prev,
      paperState: undoFold(prev.paperState)
    }))
  }, [])
  
  return {
    state,
    startLevel,
    selectTool,
    selectFoldDirection,
    doFold,
    doCut,
    unfold,
    reset,
    undo
  }
}
```

---

## 五、关卡数据

**文件**：`src/game/data/levels.ts`

```typescript
import { Level } from '@/types/game'

export const levels: Level[] = [
  // === 第一章：入门 ===
  {
    id: '1-1',
    chapter: 1,
    order: 1,
    name: '对称之美',
    description: '学习基础折叠，剪出一个对称图形',
    targetPattern: 'M 50 10 L 90 50 L 50 90 L 10 50 Z',  // 菱形
    targetImage: '/images/levels/diamond.png',
    constraints: {
      maxFolds: 1,
      maxCuts: 1,
      availableTechniques: ['basic']
    },
    difficulty: 1,
    hints: [
      '试试对角线折叠',
      '折叠后，一刀就能剪出对称图形',
      '对角线折叠后，剪一条直线'
    ]
  },
  {
    id: '1-2',
    chapter: 1,
    order: 2,
    name: '四瓣花开',
    description: '多次折叠，创造四重对称',
    targetPattern: 'M 50 10 Q 90 50 50 90 Q 10 50 50 10 Z',  // 四瓣花
    targetImage: '/images/levels/flower-4.png',
    constraints: {
      maxFolds: 2,
      maxCuts: 2,
      availableTechniques: ['basic']
    },
    difficulty: 1,
    hints: [
      '先横向折叠，再纵向折叠',
      '折叠两次后，纸张变成四层',
      '在折角处剪出花瓣形状'
    ]
  },
  // ... 更多关卡（共 35 关）
]

/**
 * 获取章节列表
 */
export function getChapters() {
  return [
    { id: 1, name: '入门', levels: levels.filter(l => l.chapter === 1) },
    { id: 2, name: '进阶', levels: levels.filter(l => l.chapter === 2) },
    { id: 3, name: '镂空', levels: levels.filter(l => l.chapter === 3) },
    { id: 4, name: '复杂', levels: levels.filter(l => l.chapter === 4) },
    { id: 5, name: '大师', levels: levels.filter(l => l.chapter === 5) }
  ]
}

/**
 * 根据 ID 获取关卡
 */
export function getLevelById(id: string): Level | undefined {
  return levels.find(l => l.id === id)
}
```

---

## 六、组件实现要点

### 6.1 PaperCanvas（纸张操作区）

**核心功能**：
- 渲染正方形红纸（SVG）
- 支持绘制折叠线（点击拖拽）
- 支持绘制刀路（点击拖拽）
- 显示已镂空区域

**实现要点**：
```typescript
// 使用 SVG 渲染纸张
<svg viewBox="0 0 100 100" className="w-full h-full">
  {/* 纸张背景 */}
  <rect x="0" y="0" width="100" height="100" fill="#DC143C" />
  
  {/* 折叠线预览 */}
  {previewFoldLine && (
    <line x1={previewFoldLine.start.x} y1={previewFoldLine.start.y}
          x2={previewFoldLine.end.x} y2={previewFoldLine.end.y}
          stroke="white" strokeDasharray="2,2" />
  )}
  
  {/* 刀路预览 */}
  {previewCutPath && (
    <path d={pathToSVG(previewCutPath)} stroke="white" fill="none" strokeDasharray="2,2" />
  )}
  
  {/* 已镂空区域 */}
  {cutoutAreas.map((area, i) => (
    <path key={i} d={pathToSVG(area)} fill="transparent" stroke="white" strokeWidth="0.5" />
  ))}
</svg>
```

### 6.2 TargetPreview（目标预览）

**核心功能**：
- 显示目标纹样
- 支持旋转查看（可选 3D 效果）
- 显示完成度进度条

**实现要点**：
```typescript
<div className="relative">
  <svg viewBox="0 0 100 100" className="w-32 h-32">
    <path d={level.targetPattern} fill="white" />
  </svg>
  
  {/* 完成度 */}
  {score && (
    <div className="absolute bottom-0 left-0 right-0 h-1 bg-gray-700">
      <div className="h-full bg-green-500" style={{ width: `${score.accuracy}%` }} />
    </div>
  )}
</div>
```

### 6.3 Toolbar（工具栏）

**核心功能**：
- 折叠按钮（展开子菜单选择方向）
- 剪裁按钮
- 展开按钮
- 撤销按钮
- 提示按钮
- 重置按钮

**实现要点**：
```typescript
<div className="flex gap-2">
  {/* 折叠 */}
  <Popover>
    <PopoverTrigger asChild>
      <Button variant="outline">
        <FoldIcon /> 折叠
      </Button>
    </PopoverTrigger>
    <PopoverContent>
      <div className="grid grid-cols-2 gap-2">
        <Button onClick={() => doFold('horizontal')}>横向</Button>
        <Button onClick={() => doFold('vertical')}>纵向</Button>
        <Button onClick={() => doFold('diagonal-left')}>左对角</Button>
        <Button onClick={() => doFold('diagonal-right')}>右对角</Button>
      </div>
    </PopoverContent>
  </Popover>
  
  {/* 剪裁 */}
  <Button onClick={() => selectTool('cut')}>
    <ScissorsIcon /> 剪裁
  </Button>
  
  {/* 展开 */}
  <Button onClick={unfold} disabled={state.paperState.cuts.length === 0}>
    <UnfoldIcon /> 展开
  </Button>
  
  {/* 其他按钮... */}
</div>
```

---

## 七、开发执行顺序

### Phase 1：基础框架（P0）

```
Step 1: 创建类型定义
        ↓
Step 2: 实现几何计算工具
        ↓
Step 3: 实现折叠引擎
        ↓
Step 4: 实现剪裁引擎
        ↓
Step 5: 实现展开引擎
        ↓
Step 6: 实现评分引擎
```

### Phase 2：状态管理（P0）

```
Step 7: 实现 useGameState hook
        ↓
Step 8: 创建关卡数据
```

### Phase 3：UI 组件（P0）

```
Step 9: 实现 PaperCanvas 组件
        ↓
Step 10: 实现 TargetPreview 组件
        ↓
Step 11: 实现 Toolbar 组件
        ↓
Step 12: 实现 ScoreDisplay 组件
        ↓
Step 13: 实现 GameContainer 组件（组装以上组件）
        ↓
Step 14: 实现 LevelSelect 组件
        ↓
Step 15: 实现 ModeSelect 组件
```

### Phase 4：整合（P0）

```
Step 16: 替换 InteractiveSection 为游戏入口
        ↓
Step 17: 测试前 10 关
        ↓
Step 18: 修复 bug，优化体验
```

### Phase 5：完整体验（P1）

```
Step 19: 实现曲线刀路
        ↓
Step 20: 实现进阶技法
        ↓
Step 21: 完成全部 35 关数据
        ↓
Step 22: 实现挑战模式
        ↓
Step 23: 实现自由模式
```

---

## 八、关键注意事项

### 8.1 性能优化

| 问题 | 解决方案 |
|------|---------|
| 频繁重渲染 | 使用 `useMemo` 缓存计算结果，使用 `React.memo` 包装组件 |
| SVG 操作卡顿 | 限制刀路点数（最多 50 个点），使用 `requestAnimationFrame` |
| 展开计算慢 | 使用 Web Worker 或分帧计算 |

### 8.2 交互体验

| 场景 | 处理方式 |
|------|---------|
| 折叠线绘制 | 实时预览，松手确认 |
| 刀路绘制 | 实时预览，点击"确认剪裁"按钮执行 |
| 展开动画 | CSS 动画，逐层展开，每层 300ms |
| 评分展示 | 先显示完成度，再显示星级，有庆祝动画 |

### 8.3 边界情况

| 情况 | 处理方式 |
|------|---------|
| 折叠次数用尽 | 折叠按钮禁用，提示"已达到折叠上限" |
| 刀数用尽 | 剪裁按钮禁用，提示"已达到刀数上限" |
| 未剪裁就展开 | 提示"请先进行剪裁" |
| 评分过低（< 50%） | 提示"继续努力，再试一次" |

### 8.4 移动端适配

| 适配点 | 方案 |
|--------|------|
| 触摸绘制 | 使用 `touchstart/touchmove/touchend` 事件 |
| 工具栏位置 | 底部固定，避免被键盘遮挡 |
| 目标预览 | 可滑动收起，节省空间 |
| 按钮尺寸 | 最小 44px × 44px |

---

## 九、测试要点

### 9.1 单元测试

- [ ] 几何计算函数正确性
- [ ] 折叠计算正确性
- [ ] 展开计算正确性
- [ ] 评分计算正确性

### 9.2 集成测试

- [ ] 完整游戏流程（开始 → 操作 → 展开 → 评分）
- [ ] 关卡切换
- [ ] 模式切换

### 9.3 手动测试

- [ ] 桌面端 Chrome/Firefox/Safari
- [ ] 移动端 iOS Safari / Android Chrome
- [ ] 触摸交互流畅度
- [ ] 动画流畅度

---

## 十、与原网站的整合方式

### 10.1 入口位置

替换 `InteractiveSection.tsx`，但保留原有的视觉风格（红色背景 + 白色内容）。

### 10.2 数据共享

- 游戏中解锁的纹样 ID 存入 `localStorage`
- 网站的"纹样图鉴"读取 `localStorage` 显示解锁状态

### 10.3 样式统一

- 使用网站已有的 Tailwind 配置
- 复用 `paper-*` 和 `rice-*` 色值
- 字体使用 `Noto Serif SC` 和 `Noto Sans SC`

---

<!-- ## 附录：简化版 MVP 实现方案

如果完整版开发周期过长，可先实现简化版 MVP：

| 功能 | 简化方式 |
|------|---------|
| 折叠 | 仅支持横向/纵向对折，不支持对角线 |
| 刀路 | 仅支持直线，不支持曲线 |
| 技法 | 仅保留基础剪，其他技法后续迭代 |
| 关卡 | 仅实现前 10 关 |
| 模式 | 仅实现闯关模式 |

MVP 开发周期预估：2-3 天。 -->
