# 「纸间万象」产品开发文档

> **本文档面向 AI 开发执行者**。阅读本文档后，AI 应能独立完成整个网站的前端开发，无需额外澄清。
>
> **上游文档**：《非遗独立主题网站 · 产品调研与定位文档》
> **下游交付**：可运行的 Next.js + Tailwind CSS 项目

---

## 0. 项目总览

| 项 | 值 |
|----|-----|
| 项目名称 | 纸间万象（Paper Universe） |
| 项目定位 | 以中国剪纸为主题的沉浸式综合型网站 |
| 技术栈 | Next.js 14+ (App Router) + TypeScript + Tailwind CSS |
| 后端 | 无。所有数据硬编码为 TypeScript 常量 |
| 部署 | Vercel（或任意支持 Next.js 的静态托管） |
| 页面结构 | 单页应用（SPA），所有内容在一个长滚动页面中呈现 |

---

## 1. 项目目录结构

```
paper-universe/
├── public/
│   ├── images/                    # 静态图片资源（AI 生成的剪纸图案、传承人照片等）
│   │   ├── hero/                  # 首屏相关图片
│   │   ├── history/               # 历史章节配图
│   │   ├── styles/                # 流派代表作品图
│   │   ├── patterns/              # 纹样 SVG/PNG
│   │   ├── artisans/              # 传承人照片
│   │   └── modern/                # 当代应用案例图
│   └── fonts/                     # 自定义字体文件（如有）
│
├── src/
│   ├── app/
│   │   ├── layout.tsx             # 根布局（字体、全局样式、metadata）
│   │   ├── page.tsx               # 唯一页面：组装所有 Section 组件
│   │   └── globals.css            # 全局 CSS（Tailwind 指令 + 自定义动画 keyframes）
│   │
│   ├── components/
│   │   ├── sections/              # 页面各章节组件（按滚动顺序排列）
│   │   │   ├── HeroSection.tsx        # 模块 1：沉浸式首屏
│   │   │   ├── HistorySection.tsx     # 模块 2-章1：一张纸的起源
│   │   │   ├── StylesSection.tsx      # 模块 2-章2：剪刀下的中国
│   │   │   ├── PatternsSection.tsx    # 模块 2-章3：纹样里的密码
│   │   │   ├── ArtisansSection.tsx    # 模块 2-章4：手艺人的坚守
│   │   │   ├── FutureSection.tsx      # 模块 2-章5：纸上的未来
│   │   │   ├── InteractiveSection.tsx # 模块 3：虚拟剪纸体验
│   │   │   ├── GallerySection.tsx     # 模块 4：流派图鉴
│   │   │   ├── ModernSection.tsx      # 模块 6：当代应用展示
│   │   │   └── FooterSection.tsx      # 模块 7：页脚
│   │   │
│   │   ├── ui/                    # 通用 UI 组件
│   │   │   ├── ScrollReveal.tsx       # 滚动触发渐入动画容器
│   │   │   ├── ParallaxLayer.tsx      # 视差层组件
│   │   │   ├── SectionTitle.tsx       # 章节标题组件
│   │   │   ├── PatternCard.tsx        # 纹样卡片
│   │   │   ├── StyleCard.tsx          # 流派卡片
│   │   │   ├── ArtisanCard.tsx        # 传承人卡片
│   │   │   ├── ModernCard.tsx         # 当代应用卡片
│   │   │   ├── ScrollProgress.tsx     # 顶部滚动进度条
│   │   │   └── ScrollIndicator.tsx    # 底部滚动提示箭头
│   │   │
│   │   └── interactive/          # 互动体验专用组件
│   │       ├── FoldCutExperience.tsx  # 子功能 3.1：折叠剪裁体验
│   │       ├── PatternComposer.tsx    # 子功能 3.2：纹样搭配器
│   │       └── ColorLab.tsx           # 子功能 3.3：色彩实验室
│   │
│   ├── data/                      # 硬编码数据（TypeScript 常量）
│   │   ├── history.ts             # 历史时间轴数据
│   │   ├── styles.ts              # 流派数据
│   │   ├── patterns.ts            # 纹样数据
│   │   ├── artisans.ts            # 传承人数据
│   │   ├── modernApps.ts          # 当代应用案例数据
│   │   └── colors.ts              # 色彩方案数据
│   │
│   ├── hooks/                     # 自定义 React Hooks
│   │   ├── useScrollReveal.ts     # 滚动进入视口检测
│   │   └── useScrollProgress.ts   # 滚动进度计算
│   │
│   ├── lib/                       # 工具函数
│   │   └── cn.ts                  # className 合并工具（clsx + tailwind-merge）
│   │
│   └── types/                     # TypeScript 类型定义
│       └── index.ts               # 所有数据类型
│
├── tailwind.config.ts             # Tailwind 配置（自定义主题色、字体、动画）
├── tsconfig.json
├── next.config.ts
├── package.json
└── README.md
```

---

## 2. 技术规范

### 2.1 初始化命令

```bash
npx create-next-app@latest paper-universe --typescript --tailwind --eslint --app --src-dir --no-import-alias
cd paper-universe
npm install clsx tailwind-merge
```

### 2.2 核心依赖

| 包名 | 用途 | 必需 |
|------|------|------|
| `next` | 框架 | ✅ |
| `react` / `react-dom` | UI 库 | ✅ |
| `typescript` | 类型安全 | ✅ |
| `tailwindcss` | 样式 | ✅ |
| `clsx` | 条件 className | ✅ |
| `tailwind-merge` | Tailwind className 冲突合并 | ✅ |
| `framer-motion` | 高级动画（可选，非必需） | ❌ |

> **注意**：不安装任何后端相关包、状态管理库、UI 组件库。保持依赖极简。

### 2.3 TypeScript 严格模式

`tsconfig.json` 必须开启：
```json
{
  "compilerOptions": {
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true
  }
}
```

---

## 3. 设计系统（Design Tokens）

### 3.1 色彩体系

在 `tailwind.config.ts` 中扩展：

```typescript
// tailwind.config.ts
import type { Config } from 'tailwindcss'

const config: Config = {
  content: ['./src/**/*.{ts,tsx}'],
  theme: {
    extend: {
      colors: {
        // 主色调 —— 剪纸红
        paper: {
          50:  '#fef2f2',
          100: '#fee2e2',
          200: '#fecaca',
          300: '#fca5a5',
          400: '#f87171',
          500: '#DC2626',  // 主色：中国红
          600: '#B91C1C',
          700: '#991B1B',
          800: '#7F1D1D',
          900: '#450a0a',
          950: '#2a0505',  // 深红/暗红（首屏背景）
        },
        // 辅助色 —— 宣纸米色
        rice: {
          50:  '#fdfcfb',
          100: '#f5f0e8',
          200: '#ebe0cc',
          300: '#ddd0b5',
          400: '#c9b896',
        },
        // 辅助色 —— 墨色
        ink: {
          700: '#374151',
          800: '#1f2937',
          900: '#111827',
          950: '#0a0a0a',
        },
        // 点缀色 —— 金色（用于传承人章节等）
        gold: {
          400: '#facc15',
          500: '#eab308',
          600: '#ca8a04',
        },
      },
    },
  },
  plugins: [],
}
export default config
```

### 3.2 字体规范

在 `src/app/layout.tsx` 中配置：

```typescript
import { Noto_Serif_SC, Noto_Sans_SC } from 'next/font/google'

const notoSerif = Noto_Serif_SC({
  subsets: ['latin'],
  weight: ['400', '700', '900'],
  variable: '--font-serif',
  display: 'swap',
})

const notoSans = Noto_Sans_SC({
  subsets: ['latin'],
  weight: ['300', '400', '500', '700'],
  variable: '--font-sans',
  display: 'swap',
})
```

| 用途 | 字体 | 字重 | Tailwind class |
|------|------|------|---------------|
| 章节大标题 | Noto Serif SC | 900 | `font-serif font-black` |
| 副标题/引言 | Noto Serif SC | 400 | `font-serif` |
| 正文 | Noto Sans SC | 400 | `font-sans` |
| 辅助文字/标签 | Noto Sans SC | 300 | `font-sans font-light` |

### 3.3 间距与排版

| 元素 | 移动端 | 桌面端 |
|------|--------|--------|
| 章节上下 padding | `py-16 px-6` | `py-24 px-8` |
| 章节标题字号 | `text-3xl` | `text-5xl` |
| 章节副标题字号 | `text-lg` | `text-xl` |
| 正文字号 | `text-base` | `text-lg` |
| 卡片间距 | `gap-4` | `gap-8` |
| 最大内容宽度 | 100% | `max-w-6xl mx-auto` |

### 3.4 圆角与阴影

```css
/* 剪纸风格：硬朗的直角为主，仅在特定元素使用微圆角 */
/* 卡片：无圆角或 rounded-sm（2px），模拟纸张的锐利边缘 */
/* 按钮：rounded-sm */
/* 模态框：rounded-none（保持剪纸的锐利感） */
```

---

## 4. 全局样式与动画

### 4.1 `globals.css` 核心内容

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  html {
    scroll-behavior: smooth;
    @apply bg-ink-950 text-rice-100;
  }

  body {
    font-family: var(--font-sans), sans-serif;
    overflow-x: hidden;
  }

  ::selection {
    @apply bg-paper-500 text-white;
  }
}

@layer utilities {
  /* 滚动触发渐入动画的三个阶段 */
  .reveal-hidden {
    opacity: 0;
    transform: translateY(40px);
    transition: opacity 0.8s ease-out, transform 0.8s ease-out;
  }

  .reveal-visible {
    opacity: 1;
    transform: translateY(0);
  }

  /* 剪纸风格装饰边框 */
  .paper-border {
    border: 2px solid paper-500;
    box-shadow: 4px 4px 0 paper-500;
  }
}
```

### 4.2 自定义动画 Keyframes

在 `tailwind.config.ts` 的 `extend.keyframes` 中定义：

```typescript
keyframes: {
  // 纸张展开（首屏）
  'paper-unfold': {
    '0%':   { transform: 'scaleX(0)', opacity: '0' },
    '50%':  { transform: 'scaleX(1)', opacity: '1' },
    '100%': { transform: 'scaleX(1)', opacity: '1' },
  },
  // 标题逐字显现
  'title-reveal': {
    '0%':   { opacity: '0', transform: 'translateY(20px)' },
    '100%': { opacity: '1', transform: 'translateY(0)' },
  },
  // 呼吸动画（scroll indicator）
  'breathe': {
    '0%, 100%': { transform: 'translateY(0)', opacity: '0.6' },
    '50%':      { transform: 'translateY(8px)', opacity: '1' },
  },
  // 剪刀开合
  'scissor-snip': {
    '0%, 100%': { transform: 'rotate(0deg)' },
    '25%':      { transform: 'rotate(-15deg)' },
    '75%':      { transform: 'rotate(15deg)' },
  },
  // 纸张翻转展开
  'paper-flip': {
    '0%':   { transform: 'scaleX(0) rotateY(0deg)' },
    '60%':  { transform: 'scaleX(1.1) rotateY(10deg)' },
    '100%': { transform: 'scaleX(1) rotateY(0deg)' },
  },
  // 淡入上移（通用）
  'fade-up': {
    '0%':   { opacity: '0', transform: 'translateY(30px)' },
    '100%': { opacity: '1', transform: 'translateY(0)' },
  },
  // 横向滑入
  'slide-in-left': {
    '0%':   { opacity: '0', transform: 'translateX(-40px)' },
    '100%': { opacity: '1', transform: 'translateX(0)' },
  },
  'slide-in-right': {
    '0%':   { opacity: '0', transform: 'translateX(40px)' },
    '100%': { opacity: '1', transform: 'translateX(0)' },
  },
},
animation: {
  'paper-unfold':    'paper-unfold 1.2s ease-out forwards',
  'title-reveal':    'title-reveal 0.8s ease-out forwards',
  'breathe':         'breathe 2s ease-in-out infinite',
  'scissor-snip':    'scissor-snip 1.5s ease-in-out infinite',
  'paper-flip':      'paper-flip 1s ease-out forwards',
  'fade-up':         'fade-up 0.8s ease-out forwards',
  'slide-in-left':   'slide-in-left 0.8s ease-out forwards',
  'slide-in-right':  'slide-in-right 0.8s ease-out forwards',
},
```

---

## 5. 通用 UI 组件规格

### 5.1 ScrollReveal（滚动触发渐入）

**文件**：`src/components/ui/ScrollReveal.tsx`

**功能**：包裹子元素，当元素进入视口时触发渐入动画。

**Props**：
```typescript
interface ScrollRevealProps {
  children: React.ReactNode
  className?: string
  delay?: number        // 延迟 ms，默认 0
  direction?: 'up' | 'left' | 'right'  // 默认 'up'
}
```

**实现要点**：
- 使用 `useRef` + `IntersectionObserver`
- 进入视口时添加 `reveal-visible` class
- 支持 `delay` 参数实现交错动画（stagger）
- `threshold: 0.15`（元素 15% 可见时触发）

---

### 5.2 SectionTitle（章节标题）

**文件**：`src/components/ui/SectionTitle.tsx`

**Props**：
```typescript
interface SectionTitleProps {
  title: string           // 中文标题
  subtitle?: string       // 英文副标题
  description?: string    // 一句话描述
  align?: 'left' | 'center'  // 默认 'center'
  light?: boolean         // true = 白色文字（深色背景），false = 深色文字（浅色背景）
}
```

**视觉规格**：
- 标题：`text-3xl md:text-5xl font-serif font-black`
- 副标题：`text-sm md:text-base font-sans font-light tracking-widest uppercase text-paper-400`
- 描述：`text-base md:text-lg font-sans mt-4 max-w-2xl mx-auto`
- 装饰线：标题下方一条 `w-16 h-0.5 bg-paper-500 mx-auto mt-4` 的红色细线

---

### 5.3 ScrollProgress（顶部进度条）

**文件**：`src/components/ui/ScrollProgress.tsx`

**功能**：页面顶部固定一条红色细线，宽度随滚动进度从 0% 到 100%。

**实现**：
- `position: fixed; top: 0; left: 0; z-index: 50`
- 高度 `h-0.5`（2px）
- 背景 `bg-paper-500`
- 宽度通过 `useScrollProgress` hook 计算

---

### 5.4 ScrollIndicator（底部滚动提示）

**文件**：`src/components/ui/ScrollIndicator.tsx`

**视觉**：一把剪刀 SVG 图标 + "向下滚动" 文字，带呼吸动画。

---

## 6. 各 Section 组件详细规格

### 6.1 HeroSection（沉浸式首屏）

**文件**：`src/components/sections/HeroSection.tsx`

**布局**：全屏（`min-h-screen flex flex-col items-center justify-center relative overflow-hidden`）

**视觉层次**（从底到顶）：

```
┌─────────────────────────────────────────┐
│  Layer 0: 背景渐变                       │
│  bg-gradient-to-b from-paper-950 to-ink-950 │
│                                         │
│  Layer 1: 装饰性剪纸纹样（低透明度）       │
│  几个剪纸 SVG 散落，opacity-10           │
│  轻微的 CSS animation 缓慢旋转/漂浮       │
│                                         │
│  Layer 2: 中央白色"纸张"                  │
│  一个白色矩形，初始 scaleX(0)            │
│  动画：paper-unfold 1.2s ease-out        │
│  尺寸：w-64 h-80 md:w-96 md:h-[28rem]   │
│  带轻微阴影：shadow-2xl                  │
│                                         │
│  Layer 3: 纸上方的标题                    │
│  「纸间万象」font-serif font-black       │
│  text-4xl md:text-7xl text-white         │
│  动画：title-reveal，延迟 1s             │
│                                         │
│  Layer 4: 副标题                         │
│  "Between the cuts, a universe unfolds"  │
│  font-sans font-light text-rice-300      │
│  text-sm md:text-base tracking-wider     │
│  动画：fade-up，延迟 1.4s                │
│                                         │
│  Layer 5: Scroll Indicator               │
│  固定在底部居中，animate-breathe          │
│  剪刀图标 + "scroll" 文字                │
└─────────────────────────────────────────┘
```

**关键实现细节**：
- 装饰性剪纸纹样用内联 SVG，3-4 个不同图案，`absolute` 定位散落在背景中
- 纸张展开动画结束后，纸张可以渐隐（`opacity transition to opacity-0`），让标题更突出
- 整个 Section 不需要滚动交互，纯入场动画

---

### 6.2 HistorySection（一张纸的起源）

**文件**：`src/components/sections/HistorySection.tsx`

**背景色**：`bg-rice-50`（浅色章节，文字用深色）

**布局**：

```
SectionTitle: "一张纸的起源" / "Origin of Paper-cutting"

时间轴（垂直排列，桌面端左右交替）：

  ●─── 公元前 105 年 ────
  │    蔡伦改进造纸术，纸张的普及为剪纸提供了物质基础
  │    [配图：古代造纸场景插画]
  │
  ●─── 南北朝（公元 420-589 年）───
  │    目前发现的最早剪纸实物，出土于新疆吐鲁番
  │    [配图：出土剪纸文物]
  │
  ●─── 唐宋时期 ────
  │    剪纸广泛用于节庆装饰、祭祀礼仪、窗花张贴
  │    [配图：唐代剪纸风格复原图]
  │
  ●─── 明清时期 ────
  │    剪纸艺术达到鼎盛，形成各地流派，成为民间最重要的装饰艺术之一
  │    [配图：明清剪纸精品]
  │
  ●─── 2009 年 ────
  │    中国剪纸入选联合国教科文组织人类非物质文化遗产代表作名录
  │    [配图：UNESCO 证书/标志]
  │
  ●─── 当代 ────
       剪纸与数字艺术、品牌设计、国际时尚跨界融合，焕发新生
       [配图：当代剪纸创新作品]
```

**实现要点**：
- 时间轴用 `relative` 容器 + `absolute` 的竖线（`left-1/2 w-0.5 h-full bg-paper-300`）
- 每个节点用 `relative` + `w-3 h-3 rounded-full bg-paper-500` 圆点
- 移动端：时间轴靠左（`left-4`），内容全部在右侧
- 桌面端：时间轴居中，内容左右交替（奇数 `text-right pr-12`，偶数 `text-left pl-12`）
- 每个节点用 `ScrollReveal` 包裹，`delay` 递增实现交错动画

**数据结构**（`src/data/history.ts`）：

```typescript
export interface HistoryEvent {
  year: string           // "公元前 105 年"
  title: string          // "蔡伦改进造纸术"
  description: string    // 详细描述
  image: string          // 图片路径 "/images/history/xxx.png"
}

export const historyEvents: HistoryEvent[] = [
  {
    year: "公元前 105 年",
    title: "造纸术的诞生",
    description: "蔡伦改进造纸术，纸张的普及为剪纸提供了物质基础。最早的剪纸形式——"剪桐封弟"的故事可追溯至西周时期。",
    image: "/images/history/paper-invention.png",
  },
  // ... 其余事件
]
```

---

### 6.3 StylesSection（剪刀下的中国）

**文件**：`src/components/sections/StylesSection.tsx`

**背景色**：`bg-ink-900`（深色章节，文字用浅色）

**布局**：SectionTitle + 流派卡片网格

**卡片规格**：

```
┌──────────────────────────────┐
│  [流派代表作品图]              │  aspect-ratio: 4/3
│  h-48 md:h-56 object-cover   │
├──────────────────────────────┤
│  流派名称                      │  text-xl font-serif font-bold
│  地域标签                      │  text-xs font-light text-paper-400
│  一句话风格描述                 │  text-sm text-rice-300 mt-2
│  风格关键词标签                 │  flex flex-wrap gap-2 mt-3
│  [粗犷] [豪放] [红色系]        │  text-xs px-2 py-0.5 border border-paper-700
└──────────────────────────────┘
```

- 桌面端：`grid grid-cols-2 lg:grid-cols-3 gap-8`
- 移动端：`grid grid-cols-1 gap-6`
- hover：卡片边框从 `border-transparent` 过渡到 `border-paper-500`，`transform: translateY(-4px)`

**数据结构**（`src/data/styles.ts`）：

```typescript
export interface PaperStyle {
  id: string
  name: string           // "陕北剪纸"
  region: string         // "陕西 · 延安"
  description: string    // 一句话风格描述
  tags: string[]         // ["粗犷", "豪放", "红色系"]
  image: string          // 图片路径
  detail: string         // 展开后的详细描述（200-300 字）
  features: string[]     // 风格特征列表
}

export const paperStyles: PaperStyle[] = [
  {
    id: "shanbei",
    name: "陕北剪纸",
    region: "陕西 · 延安、安塞",
    description: "粗犷豪放，线条浑厚有力，题材多取自黄土高原的农耕生活与民间传说。",
    tags: ["粗犷", "豪放", "红色系", "民俗"],
    image: "/images/styles/shanbei.png",
    detail: "陕北剪纸是中国北方剪纸的代表...",
    features: ["线条粗犷有力", "构图饱满对称", "色彩以红色为主", "题材贴近农耕生活"],
  },
  // ... 其余流派（蔚县、扬州、佛山、广灵、和林格尔）
]
```

**点击交互**：点击卡片 → 内联展开详情区域（`max-height` + `overflow-hidden` + `transition`），展示详细描述和风格特征列表。

---

### 6.4 PatternsSection（纹样里的密码）

**文件**：`src/components/sections/PatternsSection.tsx`

**背景色**：`bg-paper-500`（红色背景，白色内容——最"剪纸"的章节）

**布局**：SectionTitle（白色文字）+ 纹样网格

**纹样卡片规格**：

```
┌──────────────────────────────┐
│  [纹样 SVG 图案]              │  纯色背景上的白色剪纸图案
│  w-full aspect-square        │
│  bg-white/10 rounded-none    │
├──────────────────────────────┤
│  纹样名称                      │  text-lg font-serif text-white
│  寓意                          │  text-sm text-rice-200 mt-1
│  "蝙蝠 = 福"                  │
└──────────────────────────────┘
```

- 网格：`grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4 md:gap-6`
- hover：纹样 SVG 轻微旋转（`rotate-3`）+ 放大（`scale-105`）
- 纹样图案用内联 SVG 绘制（AI 生成简单的剪纸风格 SVG path）

**数据结构**（`src/data/patterns.ts`）：

```typescript
export interface Pattern {
  id: string
  name: string           // "蝙蝠纹"
  meaning: string        // "福（谐音）"
  description: string    // 详细寓意说明
  svgPath: string        // SVG path 数据（用于内联渲染）
  category: string       // "吉祥纹" | "生肖" | "花鸟" | "人物"
}

export const patterns: Pattern[] = [
  {
    id: "bat",
    name: "蝙蝠纹",
    meaning: "福",
    description: "蝙蝠的"蝠"与"福"谐音，是最常见的吉祥纹样之一。五只蝙蝠围绕"寿"字，寓意"五福捧寿"。",
    svgPath: "M...",  // 剪纸风格的蝙蝠 SVG path
    category: "吉祥纹",
  },
  // ... 其余纹样（鱼、牡丹、喜鹊、龙凤、石榴、莲花、仙鹤等）
]
```

---

### 6.5 ArtisansSection（手艺人的坚守）

**文件**：`src/components/sections/ArtisansSection.tsx`

**背景色**：`bg-rice-100`（暖米色背景）

**布局**：SectionTitle + 横向滚动卡片

**卡片规格**：

```
┌──────────────────────────────┐
│  [传承人照片]                  │  w-full h-64 object-cover
│                              │  带剪纸风格红色边框装饰
├──────────────────────────────┤
│  姓名                         │  text-xl font-serif font-bold
│  流派 · 从业年限               │  text-sm text-ink-700
│  "手艺人的话"                  │  text-base font-serif italic
│  用引号包裹，手写体风格          │  text-paper-600 mt-3
└──────────────────────────────┘
```

- 容器：`flex overflow-x-auto snap-x snap-mandatory gap-6 pb-4`（隐藏滚动条：`scrollbar-hide`）
- 每张卡片：`snap-center min-w-[300px] md:min-w-[360px]`
- hover：`-translate-y-2 shadow-xl`

**数据结构**（`src/data/artisans.ts`）：

```typescript
export interface Artisan {
  id: string
  name: string           // "高金爱"
  style: string          // "陕北剪纸"
  years: number          // 从业年限
  quote: string          // "手艺人的话"
  image: string          // 照片路径
  bio: string            // 简短生平（100 字）
}

export const artisans: Artisan[] = [
  {
    id: "gao-jinai",
    name: "高金爱",
    style: "陕北剪纸",
    years: 60,
    quote: "剪刀一拿就是一辈子，纸上的世界比外面的还大。",
    image: "/images/artisans/gao-jinai.png",
    bio: "陕西延安安塞人，国家级非遗传承人...",
  },
  // ... 2-3 位传承人
]
```

---

### 6.6 FutureSection（纸上的未来）

**文件**：`src/components/sections/FutureSection.tsx`

**背景色**：`bg-ink-950`（深色背景，现代感）

**布局**：SectionTitle + 当代应用案例网格（带分类标签筛选）

**筛选标签栏**：
```
[全部] [品牌设计] [包装] [服装] [空间装饰] [数字艺术]
```
- 选中状态：`bg-paper-500 text-white`
- 未选中：`border border-rice-400 text-rice-300 hover:border-paper-500`

**案例卡片规格**：
```
┌──────────────────────────────┐
│  [案例图片]                    │  aspect-ratio: 16/10
│  h-48 object-cover            │
├──────────────────────────────┤
│  案例标题                      │  text-base font-serif
│  简短说明                      │  text-sm text-rice-400 mt-1
└──────────────────────────────┘
```

- 网格：`grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`
- 筛选逻辑：`useState<string>('全部')`，过滤 `modernApps` 数据

**数据结构**（`src/data/modernApps.ts`）：

```typescript
export interface ModernApp {
  id: string
  title: string          // "剪纸 × 星巴克春节限定"
  description: string    // 简短说明
  category: string       // "品牌设计" | "包装" | "服装" | "空间装饰" | "数字艺术"
  image: string          // 图片路径
}

export const modernApps: ModernApp[] = [
  {
    id: "starbucks",
    title: "剪纸 × 星巴克春节限定",
    description: "星巴克与中国剪纸艺术家合作，推出春节限定杯套和包装，将传统窗花元素与现代咖啡文化融合。",
    category: "品牌设计",
    image: "/images/modern/starbucks.png",
  },
  // ... 6-9 个案例
]
```

---

### 6.7 InteractiveSection（虚拟剪纸体验）

**文件**：`src/components/sections/InteractiveSection.tsx`

**背景色**：`bg-rice-50`（浅色背景）

**布局**：SectionTitle + 三个子体验的 Tab 切换

**Tab 栏**：
```
[折叠剪裁] [纹样搭配] [色彩实验室]
```
- 选中：`bg-paper-500 text-white font-bold`
- 未选中：`bg-white text-ink-800 hover:bg-rice-200`

---

#### 子功能 3.1：FoldCutExperience（折叠剪裁体验）

**文件**：`src/components/interactive/FoldCutExperience.tsx`

**交互流程**：

```
步骤 1: 选择折叠方式
┌─────────┐  ┌─────────┐  ┌─────────┐
│  对折    │  │  四折    │  │  八折    │
│  [图示]  │  │  [图示]  │  │  [图示]  │
└─────────┘  └─────────┘  └─────────┘
  选中态：border-paper-500 bg-paper-50

步骤 2: 选择剪裁图案（预设 3-4 种）
┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐
│ [预览图] │  │ [预览图] │  │ [预览图] │  │ [预览图] │
│  窗花    │  │  喜字    │  │  蝴蝶    │  │  花朵    │
└─────────┘  └─────────┘  └─────────┘  └─────────┘

步骤 3: 点击「展开」按钮
→ 播放 paper-flip 动画
→ 显示最终剪纸结果（一张精美的剪纸图案图片）
→ 下方显示："恭喜！你创作了一幅 [窗花] 风格的剪纸作品"
```

**State**：
```typescript
const [foldType, setFoldType] = useState<'half' | 'quarter' | 'eighth'>('half')
const [patternId, setPatternId] = useState<string | null>(null)
const [isUnfolded, setIsUnfolded] = useState(false)
```

**实现要点**：
- 折叠方式选择：三个按钮，点击切换 `foldType`
- 剪裁图案选择：展示预设图案的缩略图，点击选择
- "展开"按钮：`disabled` 直到两个步骤都完成
- 展开动画：`animate-paper-flip`，动画结束后显示结果图
- 结果图根据 `foldType` + `patternId` 组合从预设数据中匹配

---

#### 子功能 3.2：PatternComposer（纹样搭配器）

**文件**：`src/components/interactive/PatternComposer.tsx`

**布局**：

```
┌──────────────────────────────────────────────┐
│  左侧：预览区（60%）    │  右侧：纹样选择区（40%）│
│                        │                      │
│  ┌──────────────────┐  │  可选纹样元素：         │
│  │                  │  │  ┌────┐ ┌────┐        │
│  │   [轮廓形状]      │  │  │蝙蝠│ │ 鱼 │        │
│  │   内含已选纹样    │  │  └────┘ └────┘        │
│  │                  │  │  ┌────┐ ┌────┐        │
│  │                  │  │  │牡丹│ │喜鹊│        │
│  └──────────────────┘  │  └────┘ └────┘        │
│                        │                      │
│  轮廓选择：              │  点击纹样 = 添加/移除   │
│  [圆形] [方形] [心形]   │                      │
│                        │  配色方案：             │
│  配色方案：              │  [传统红] [蓝印花]     │
│  [传统红] [蓝印花]      │  [黑白] [彩色]         │
│  [黑白] [彩色]          │                      │
└──────────────────────────────────────────────┘
```

**State**：
```typescript
const [shape, setShape] = useState<'circle' | 'square' | 'heart'>('circle')
const [selectedPatterns, setSelectedPatterns] = useState<string[]>([])
const [colorScheme, setColorScheme] = useState<'red' | 'blue' | 'mono' | 'colorful'>('red')
```

**配色方案定义**（`src/data/colors.ts`）：
```typescript
export const colorSchemes = {
  red:     { bg: '#DC2626', fg: '#FFFFFF', name: '传统红', desc: '最经典的剪纸配色，象征喜庆与吉祥' },
  blue:    { bg: '#1e3a5f', fg: '#FFFFFF', name: '蓝印花', desc: '江南蓝印花布风格，清新淡雅' },
  mono:    { bg: '#111827', fg: '#F5F0E8', name: '黑白极简', desc: '现代极简风格，突出剪纸的线条之美' },
  colorful:{ bg: 'linear-gradient(135deg, #DC2626, #eab308, #22c55e)', fg: '#FFFFFF', name: '现代彩色', desc: '当代创新配色，打破传统束缚' },
}
```

**实现要点**：
- 预览区：SVG 绘制轮廓 + 已选纹样元素叠加显示
- 纹样元素用 SVG `use` 引用定义好的 `<symbol>`
- 点击纹样切换选中/未选中状态（`toggle`）
- 切换配色方案时，预览区颜色通过 CSS `transition` 平滑过渡

---

#### 子功能 3.3：ColorLab（色彩实验室）

**文件**：`src/components/interactive/ColorLab.tsx`

**布局**：

```
┌──────────────────────────────────────────────┐
│  标题："同一纹样，不同灵魂"                      │
│                                              │
│  ┌────────────────────────────────────────┐  │
│  │         [大尺寸纹样预览图]               │  │
│  │         根据选中的配色方案变色            │  │
│  │         CSS transition 平滑过渡          │  │
│  └────────────────────────────────────────┘  │
│                                              │
│  配色方案选择（横向排列的色块按钮）：             │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐       │
│  │ ██红 │ │ ██蓝 │ │ ██金 │ │ ██绿 │       │
│  │ 喜庆  │ │ 青花  │ │ 皇家  │ │ 生机  │       │
│  └──────┘ └──────┘ └──────┘ └──────┘       │
│                                              │
│  当前配色说明：                                 │
│  "红色 —— 喜庆与吉祥。中国剪纸最经典的配色，    │
│   象征着热烈、欢快与祝福。"                     │
└──────────────────────────────────────────────┘
```

**State**：
```typescript
const [activeColor, setActiveColor] = useState(0)
```

**实现要点**：
- 纹样预览图使用 SVG，通过 `fill` 和 `stroke` 属性控制颜色
- 切换配色时用 `transition-all duration-500` 实现平滑过渡
- 色块按钮 `hover` 时显示 tooltip（配色名称）

---

### 6.8 GallerySection（流派图鉴）

**文件**：`src/components/sections/GallerySection.tsx`

**背景色**：`bg-rice-50`

**说明**：此模块与 StylesSection（6.3）功能重叠，建议合并到 StylesSection 中。如果需要独立展示更详细的内容，可作为 StylesSection 的展开详情。

**决策**：**不单独实现 GallerySection**，将其功能合并到 StylesSection 的卡片展开详情中。这样避免内容重复，也减少开发量。

---

### 6.9 ModernSection（当代应用展示）

**说明**：此模块与 FutureSection（6.6）功能完全一致。

**决策**：**不单独实现 ModernSection**，直接使用 FutureSection 承载当代应用展示功能。

---

### 6.10 FooterSection（页脚）

**文件**：`src/components/sections/FooterSection.tsx`

**背景色**：`bg-paper-950`

**布局**：

```
┌──────────────────────────────────────────────┐
│                                              │
│  "每一刀剪下去的，不只是纸，是时间。"           │
│  —— 一位剪纸老艺人                            │
│                                              │
│  ──────────── 分隔线 ────────────             │
│                                              │
│  技术栈：[Next.js] [Tailwind CSS] [TypeScript]│
│                                              │
│  参考资源：中国非物质文化遗产网 ihchina.cn      │
│                                              │
│  © 2026 纸间万象 | 课程作业                   │
└──────────────────────────────────────────────┘
```

---

## 7. 页面组装

**文件**：`src/app/page.tsx`

```typescript
import ScrollProgress from '@/components/ui/ScrollProgress'
import HeroSection from '@/components/sections/HeroSection'
import HistorySection from '@/components/sections/HistorySection'
import StylesSection from '@/components/sections/StylesSection'
import PatternsSection from '@/components/sections/PatternsSection'
import ArtisansSection from '@/components/sections/ArtisansSection'
import FutureSection from '@/components/sections/FutureSection'
import InteractiveSection from '@/components/sections/InteractiveSection'
import FooterSection from '@/components/sections/FooterSection'

export default function Home() {
  return (
    <main>
      <ScrollProgress />
      <HeroSection />
      <HistorySection />
      <StylesSection />
      <PatternsSection />
      <ArtisansSection />
      <InteractiveSection />
      <FutureSection />
      <FooterSection />
    </main>
  )
}
```

**Section 顺序与背景色交替**：

| 顺序 | Section | 背景色 | 文字色 |
|------|---------|--------|--------|
| 1 | HeroSection | `bg-gradient-to-b from-paper-950 to-ink-950` | 白色 |
| 2 | HistorySection | `bg-rice-50` | 深色 |
| 3 | StylesSection | `bg-ink-900` | 浅色 |
| 4 | PatternsSection | `bg-paper-500` | 白色 |
| 5 | ArtisansSection | `bg-rice-100` | 深色 |
| 6 | InteractiveSection | `bg-rice-50` | 深色 |
| 7 | FutureSection | `bg-ink-950` | 浅色 |
| 8 | FooterSection | `bg-paper-950` | 白色 |

---

## 8. 自定义 Hooks 规格

### 8.1 useScrollReveal

**文件**：`src/hooks/useScrollReveal.ts`

```typescript
import { useEffect, useRef } from 'react'

export function useScrollReveal<T extends HTMLElement>(options?: {
  threshold?: number
  rootMargin?: string
}) {
  const ref = useRef<T>(null)

  useEffect(() => {
    const el = ref.current
    if (!el) return

    const observer = new IntersectionObserver(
      ([entry]) => {
        if (entry.isIntersecting) {
          el.classList.add('reveal-visible')
          observer.unobserve(el) // 只触发一次
        }
      },
      { threshold: options?.threshold ?? 0.15, rootMargin: options?.rootMargin }
    )

    observer.observe(el)
    return () => observer.disconnect()
  }, [options?.threshold, options?.rootMargin])

  return ref
}
```

### 8.2 useScrollProgress

**文件**：`src/hooks/useScrollProgress.ts`

```typescript
import { useState, useEffect } from 'react'

export function useScrollProgress() {
  const [progress, setProgress] = useState(0)

  useEffect(() => {
    const handleScroll = () => {
      const scrollTop = window.scrollY
      const docHeight = document.documentElement.scrollHeight - window.innerHeight
      setProgress(docHeight > 0 ? (scrollTop / docHeight) * 100 : 0)
    }

    window.addEventListener('scroll', handleScroll, { passive: true })
    return () => window.removeEventListener('scroll', handleScroll)
  }, [])

  return progress
}
```

---

## 9. 工具函数

### 9.1 cn（className 合并）

**文件**：`src/lib/cn.ts`

```typescript
import { clsx, type ClassValue } from 'clsx'
import { twMerge } from 'tailwind-merge'

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```

---

## 10. 响应式断点策略

| 断点 | Tailwind 前缀 | 典型设备 | 适配策略 |
|------|-------------|---------|---------|
| < 640px | （默认） | 手机竖屏 | 单列布局、减小字号、简化动画、增大触控区域 |
| 640-767px | `sm:` | 手机横屏 | 同上 |
| 768-1023px | `md:` | 平板 | 双列布局、保留大部分动画 |
| 1024-1279px | `lg:` | 小桌面 | 完整布局 |
| ≥ 1280px | `xl:` | 大桌面 | 最大内容宽度 `max-w-6xl` |

**移动端特殊处理**：
- HeroSection：纸张尺寸缩小（`w-64 h-80`）
- 时间轴：从居中交替改为左侧单列
- 横向滚动卡片：卡片宽度缩小（`min-w-[280px]`）
- InteractiveSection：Tab 内容区域改为全宽，左右布局改为上下布局
- 隐藏滚动条：`scrollbar-hide`（需自定义 CSS）

```css
/* globals.css 中添加 */
.scrollbar-hide {
  -ms-overflow-style: none;
  scrollbar-width: none;
}
.scrollbar-hide::-webkit-scrollbar {
  display: none;
}
```

---

## 11. 图片资源规划

所有图片由 AI 生成，统一放置在 `public/images/` 对应子目录中。

| 目录 | 内容 | 数量 | 格式 | 建议尺寸 |
|------|------|------|------|---------|
| `hero/` | 装饰性剪纸纹样 SVG | 3-4 个 | SVG | 200x200 |
| `history/` | 历史章节配图 | 6 张 | PNG/WebP | 800x600 |
| `styles/` | 流派代表作品 | 6 张 | PNG/WebP | 800x600 |
| `patterns/` | 纹样图案 | 8-12 个 | SVG | 300x300 |
| `artisans/` | 传承人照片 | 3 张 | PNG/WebP | 600x800 |
| `modern/` | 当代应用案例 | 6-9 张 | PNG/WebP | 800x500 |

> **图片占位方案**：开发阶段可使用纯色背景 + 文字标注的占位图，后续替换为 AI 生成的实际图片。

---

## 12. 性能优化清单

| 优化项 | 实现方式 |
|--------|---------|
| 图片优化 | 使用 `next/image` 组件，启用 `priority`（首屏图片）和懒加载 |
| 字体优化 | 使用 `next/font/google`，自动优化加载 |
| 代码分割 | Next.js App Router 自动按路由分割（本项目为单页，无需额外配置） |
| 动画性能 | 仅使用 `transform` 和 `opacity` 触发动画（GPU 加速），避免 `width`/`height`/`top`/`left` 动画 |
| SVG 优化 | 内联关键 SVG，设置 `aria-hidden="true"` |
| 滚动监听 | Intersection Observer（非 scroll 事件），`passive: true` |
| CSS 优化 | Tailwind CSS 自动 purge 未使用的样式 |

---

## 13. 无障碍（Accessibility）

| 要求 | 实现 |
|------|------|
| 语义化 HTML | 使用 `<header>`, `<main>`, `<section>`, `<article>`, `<footer>`, `<nav>`, `<time>` |
| 图片 alt | 所有 `<img>` 必须有描述性 `alt` 文本 |
| 键盘导航 | Tab 键可遍历所有可交互元素，focus 样式清晰（`focus-visible:ring-2 ring-paper-500`） |
| 颜色对比度 | 文字与背景对比度 ≥ 4.5:1（WCAG AA） |
| 减少动画 | `@media (prefers-reduced-motion: reduce)` 时关闭所有动画 |
| ARIA 标签 | 互动组件添加 `role` 和 `aria-label` |

---

## 14. 开发执行顺序

建议按以下顺序逐步实现，每完成一个模块即可验证：

| 步骤 | 内容 | 验证标准 |
|------|------|---------|
| **Step 1** | 项目初始化（create-next-app + 依赖安装 + Tailwind 配置 + 字体配置 + 全局样式） | `npm run dev` 启动成功，页面显示自定义字体 |
| **Step 2** | 工具函数 + Hooks（cn.ts + useScrollReveal + useScrollProgress） | 单元测试通过或手动验证 |
| **Step 3** | 通用 UI 组件（ScrollReveal + SectionTitle + ScrollProgress + ScrollIndicator） | 在临时页面中渲染验证动画效果 |
| **Step 4** | HeroSection | 首屏全屏展示，纸张展开动画流畅，标题渐入 |
| **Step 5** | HistorySection | 时间轴正确渲染，滚动触发渐入，移动端/桌面端布局正确 |
| **Step 6** | StylesSection | 流派卡片网格渲染，hover 效果，点击展开详情 |
| **Step 7** | PatternsSection | 红色背景章节，纹样网格，SVG 渲染，hover 旋转效果 |
| **Step 8** | ArtisansSection | 横向滚动卡片，scroll-snap 效果 |
| **Step 9** | InteractiveSection — FoldCutExperience | 折叠选择 → 图案选择 → 展开动画 → 结果展示 |
| **Step 10** | InteractiveSection — PatternComposer | 轮廓 + 纹样选择 + 配色切换 + 实时预览 |
| **Step 11** | InteractiveSection — ColorLab | 纹样预览 + 配色切换 + 说明文字 |
| **Step 12** | FutureSection | 标签筛选 + 案例网格 + 过渡动画 |
| **Step 13** | FooterSection | 页脚内容渲染 |
| **Step 14** | 页面组装 + ScrollProgress | 所有 Section 按顺序组装，进度条工作正常 |
| **Step 15** | 响应式适配 + 无障碍 + 性能优化 | 多设备测试，Lighthouse 评分 ≥ 90 |

---

## 15. 根布局模板

**文件**：`src/app/layout.tsx`

```typescript
import type { Metadata } from 'next'
import { Noto_Serif_SC, Noto_Sans_SC } from 'next/font/google'
import './globals.css'

const notoSerif = Noto_Serif_SC({
  subsets: ['latin'],
  weight: ['400', '700', '900'],
  variable: '--font-serif',
  display: 'swap',
})

const notoSans = Noto_Sans_SC({
  subsets: ['latin'],
  weight: ['300', '400', '500', '700'],
  variable: '--font-sans',
  display: 'swap',
})

export const metadata: Metadata = {
  title: '纸间万象 | 中国剪纸数字体验',
  description: '以中国剪纸为核心主题的沉浸式数字体验网站，通过视觉叙事与互动体验，带你走进剪纸的世界。',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="zh-CN" className={`${notoSerif.variable} ${notoSans.variable}`}>
      <body className="font-sans antialiased">
        {children}
      </body>
    </html>
  )
}
```

---

## 16. TypeScript 类型汇总

**文件**：`src/types/index.ts`

```typescript
// 历史事件
export interface HistoryEvent {
  year: string
  title: string
  description: string
  image: string
}

// 流派
export interface PaperStyle {
  id: string
  name: string
  region: string
  description: string
  tags: string[]
  image: string
  detail: string
  features: string[]
}

// 纹样
export interface Pattern {
  id: string
  name: string
  meaning: string
  description: string
  svgPath: string
  category: '吉祥纹' | '生肖' | '花鸟' | '人物'
}

// 传承人
export interface Artisan {
  id: string
  name: string
  style: string
  years: number
  quote: string
  image: string
  bio: string
}

// 当代应用
export interface ModernApp {
  id: string
  title: string
  description: string
  category: '品牌设计' | '包装' | '服装' | '空间装饰' | '数字艺术'
  image: string
}

// 配色方案
export interface ColorScheme {
  bg: string
  fg: string
  name: string
  desc: string
}

// 折叠类型
export type FoldType = 'half' | 'quarter' | 'eighth'

// 剪裁图案预设
export interface CutPattern {
  id: string
  name: string
  image: string       // 预览缩略图
  resultImage: string // 展开后的结果图
  compatibleFolds: FoldType[]
}
```

---

## 17. 数据文件内容要求

每个 `src/data/*.ts` 文件需要包含完整的硬编码数据。以下是各文件需要填充的数据量：

| 文件 | 数据条数 | 每条内容要求 |
|------|---------|-------------|
| `history.ts` | 6 条 | year + title + description（50-100字）+ image 路径 |
| `styles.ts` | 6 条 | name + region + description + tags(3-4个) + image + detail(200-300字) + features(4个) |
| `patterns.ts` | 8-12 条 | name + meaning + description(50-80字) + svgPath + category |
| `artisans.ts` | 3 条 | name + style + years + quote + image + bio(100字) |
| `modernApps.ts` | 6-9 条 | title + description(30-50字) + category + image |
| `colors.ts` | 4 种配色方案 | bg + fg + name + desc |

> **AI 执行时**：所有数据内容（文字描述、SVG path 等）由 AI 根据真实剪纸文化知识生成，确保内容准确、有文化深度。图片路径先使用占位路径，后续替换为 AI 生成的实际图片。
