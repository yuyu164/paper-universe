# 纹样美化代码修改提示

> 本文档说明纹样 SVG 优化美化需要修改哪些文件，以及每个文件的具体改动内容。

---

## 涉及文件清单

| 序号 | 文件路径 | 改动级别 | 改动内容 |
|------|---------|---------|---------|
| 1 | `src/types/index.ts` | 小改 | `Pattern` 接口字段调整 |
| 2 | `src/data/patterns.ts` | 大改 | 全部 12 条纹样数据替换 |
| 3 | `src/components/sections/PatternsSection.tsx` | 中改 | SVG 渲染逻辑适配 |

---

## 文件 1：`src/types/index.ts`

**改动内容**：将 `Pattern` 接口中的 `svgPath: string` 改为 `svgContent: string`。

**原因**：优化后的纹样需要多 path 组合 + `fill-rule="evenodd"` 实现镂空效果，单个 path 的 `d` 属性值无法满足需求。改为存储完整的 SVG 内部标记。

**改动前**：
```typescript
export interface Pattern {
  id: string
  name: string
  meaning: string
  description: string
  svgPath: string        // ← 旧字段：单个 path 的 d 属性值
  category: string
}
```

**改动后**：
```typescript
export interface Pattern {
  id: string
  name: string
  meaning: string
  description: string
  svgContent: string     // ← 新字段：完整的 SVG 内部标记（可含多个 <path>）
  category: string
}
```

---

## 文件 2：`src/data/patterns.ts`

**改动内容**：全部重写。

**具体变更**：
1. 字段名 `svgPath` → `svgContent`
2. 所有 12 个纹样的 SVG 内容替换为优化后的版本
3. 新增 2 个纹样（梅花纹、铜钱纹），删除无

**数据结构示例**：
```typescript
export const patterns: Pattern[] = [
  {
    id: "bat",
    name: "蝙蝠纹",
    meaning: "福",
    description: "蝙蝠的"蝠"与"福"谐音，是最常见的吉祥纹样。五只蝙蝠围绕"寿"字，寓意"五福捧寿"。",
    svgContent: `<path fill-rule="evenodd" d="M50 10 C30 10 15 25 15 45 C15 55 20 65 30 70 L30 85 L40 75 L50 80 L60 75 L70 85 L70 70 C80 65 85 55 85 45 C85 25 70 10 50 10 Z M35 40 A3 3 0 1 0 35 46 A3 3 0 1 0 35 40 Z M65 40 A3 3 0 1 0 65 46 A3 3 0 1 0 65 40 Z" />`,
    category: "吉祥纹",
  },
  // ... 其余 11 个纹样，svgContent 同理替换
]
```

**12 个纹样完整列表**：

| id | 名称 | 寓意 | 类别 | 操作 |
|----|------|------|------|------|
| `bat` | 蝙蝠纹 | 福 | 吉祥纹 | 微调 SVG |
| `fish` | 鱼纹 | 年年有余 | 吉祥纹 | 微调 SVG |
| `peony` | 牡丹纹 | 富贵 | 花鸟 | 🔴 重做 SVG |
| `magpie` | 喜鹊纹 | 喜 | 花鸟 | 🟡 大改 SVG |
| `dragon` | 龙凤纹 | 龙凤呈祥 | 吉祥纹 | 🔴 重做 SVG |
| `pomegranate` | 石榴纹 | 多子多福 | 花鸟 | 🔴 重做 SVG |
| `lotus` | 莲花纹 | 高洁 | 花鸟 | 🟡 大改 SVG |
| `crane` | 仙鹤纹 | 长寿 | 花鸟 | 🟡 大改 SVG |
| `shou` | 寿字纹 | 长寿 | 吉祥纹 | 微调 SVG |
| `rabbit` | 兔纹 | 机敏吉祥 | 生肖 | 微调 SVG |
| `plum` | 梅花纹 | 坚韧不屈 | 花鸟 | 🆕 新增 |
| `coin` | 铜钱纹 | 招财进宝 | 吉祥纹 | 🆕 新增 |

**每个 SVG 的设计要求**：详见《纹样SVG优化美化指南.md》第 2 节。

---

## 文件 3：`src/components/sections/PatternsSection.tsx`

**改动内容**：SVG 渲染方式从单 path 改为支持完整 SVG 内容注入。

**改动前**（推测）：
```tsx
<svg viewBox="0 0 100 100" fill="currentColor">
  <path d={pattern.svgPath} />
</svg>
```

**改动后**：
```tsx
<svg
  viewBox="0 0 100 100"
  fill="currentColor"
  stroke="none"
  xmlns="http://www.w3.org/2000/svg"
  dangerouslySetInnerHTML={{ __html: pattern.svgContent }}
/>
```

**注意**：
- 使用 `dangerouslySetInnerHTML` 是因为 `svgContent` 包含完整的 SVG 子元素标记
- 由于所有数据均为硬编码（开发者自己控制的常量），不存在 XSS 风险
- 确保 `<svg>` 标签上设置 `fill="currentColor"` 和 `stroke="none"`
- `viewBox` 统一为 `"0 0 100 100"`

---

## 不需要修改的文件

| 文件 | 原因 |
|------|------|
| `src/app/page.tsx` | Section 组装顺序不变 |
| `src/app/layout.tsx` | 全局布局不变 |
| `src/app/globals.css` | 样式不变 |
| `tailwind.config.ts` | 主题配置不变 |
| 其他 Section 组件 | 与纹样无关 |
| `src/hooks/` | 无关 |
| `src/lib/` | 无关 |

---

## 修改执行顺序

**必须一次性完成所有 3 个文件的修改，不要在中间停下来运行编译或检查类型错误。**

```
Step 1: 修改 types/index.ts（接口字段 svgPath → svgContent）
Step 2: 修改 patterns.ts（12 条数据全部替换）
Step 3: 修改 PatternsSection.tsx（渲染逻辑适配）
Step 4: 全部改完后，再运行 npm run dev 验证
```

> **重要**：Step 1 改完接口后，`svgPath` 字段不再存在，Step 2 和 Step 3 中引用旧字段名的代码会出现类型报错。**这是预期行为，不要因此中断或尝试修复**。按顺序连续改完 3 个文件后，所有类型错误会自动消除。**禁止在 Step 1-3 之间执行 `npm run dev`、`tsc`、`npm run build` 或任何编译/类型检查命令。**
