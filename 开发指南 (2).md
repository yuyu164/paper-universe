# 纹样拼图游戏开发指南

> **目标读者**：AI 开发执行者
> **上游文档**：《纹样拼图游戏设计.md》
> **技术约束**：Next.js + Tailwind CSS + TypeScript，纯前端

---

## 一、项目结构

```
src/
├── components/
│   ├── sections/
│   │   └── InteractiveSection.tsx   # 游戏入口
│   │
│   └── puzzle/
│       ├── PuzzleGame.tsx           # 游戏主组件
│       ├── PuzzleBoard.tsx          # 拼图区域（网格+放置区）
│       ├── PiecePool.tsx            # 候选区（碎片池）
│       ├── PuzzlePiece.tsx          # 单个拼图碎片
│       ├── DifficultySelect.tsx     # 难度选择
│       ├── AssetPreview.tsx         # 目标预览/完成展示
│       ├── CompletionModal.tsx      # 完成弹层
│       └── PaperConfetti.tsx        # 纸屑飘落效果
│
├── hooks/
│   ├── usePuzzleGame.ts             # 游戏状态管理
│   ├── useAssetManager.ts           # 素材管理（换一换逻辑）
│   └── useImageSlicer.ts            # 图片切片
│
├── lib/
│   ├── imageUtils.ts                # Canvas 图片处理
│   └── dragUtils.ts                 # 拖拽辅助函数
│
├── types/
│   └── puzzle.ts                    # 类型定义
│
└── app/
    └── globals.css                  # 拼图相关 CSS 动画
```

---

## 二、类型定义

**文件**：`src/types/puzzle.ts`

```typescript
// 拼图块
export interface PuzzlePiece {
  id: number
  correctRow: number
  correctCol: number
  currentRow: number | null    // null 表示在候选区
  currentCol: number | null
  imageUrl: string             // Blob URL
}

// 难度
export type Difficulty = 3 | 4

// 素材状态
export type AssetStatus = 'fresh' | 'played' | 'skipped'

export interface PuzzleAsset {
  id: string
  src: string
  status: AssetStatus
  lastUsedAt: number
}

// 游戏状态
export interface GameState {
  assetId: string | null
  difficulty: Difficulty
  pieces: PuzzlePiece[]
  isCompleted: boolean
  startTime: number
  endTime: number | null
}

// 拖拽状态
export interface DragState {
  pieceId: number | null
  isDragging: boolean
  dragOffset: { x: number; y: number }
}

// 放置判定结果
export type DropResult = 
  | { type: 'success'; row: number; col: number }
  | { type: 'fail'; reason: 'out-of-range' | 'wrong-position' | 'occupied-wrong' }
  | { type: 'swap'; row: number; col: number; targetPieceId: number }
```

---

## 三、核心功能实现

### 3.1 图片切片（Canvas）

**文件**：`src/lib/imageUtils.ts`

```typescript
/**
 * 将图片切片为拼图块
 * @param imageSrc 图片路径
 * @param difficulty 3 或 4
 * @returns 每个拼图块的 Blob URL 数组
 */
export async function sliceImage(
  imageSrc: string,
  difficulty: Difficulty
): Promise<string[]> {
  return new Promise((resolve, reject) => {
    const img = new Image()
    img.crossOrigin = 'anonymous'
    img.onload = () => {
      const canvas = document.createElement('canvas')
      const ctx = canvas.getContext('2d')!
      
      // 1. 确定正方形尺寸（取短边）
      const size = Math.min(img.width, img.height)
      
      // 2. 计算切片尺寸
      const pieceSize = size / difficulty
      
      // 3. 计算裁剪起点（居中裁剪）
      const startX = (img.width - size) / 2
      const startY = (img.height - size) / 2
      
      // 4. 生成每个切片
      const urls: string[] = []
      
      for (let row = 0; row < difficulty; row++) {
        for (let col = 0; col < difficulty; col++) {
          canvas.width = pieceSize
          canvas.height = pieceSize
          
          ctx.drawImage(
            img,
            startX + col * pieceSize,
            startY + row * pieceSize,
            pieceSize,
            pieceSize,
            0,
            0,
            pieceSize,
            pieceSize
          )
          
          canvas.toBlob((blob) => {
            if (blob) {
              urls.push(URL.createObjectURL(blob))
            }
          }, 'image/jpeg', 0.9)
        }
      }
      
      resolve(urls)
    }
    img.onerror = reject
    img.src = imageSrc
  })
}

/**
 * 打乱数组（Fisher-Yates 算法）
 */
export function shuffleArray<T>(array: T[]): T[] {
  const arr = [...array]
  for (let i = arr.length - 1; i > 0; i--) {
    const j = Math.floor(Math.random() * (i + 1))
    ;[arr[i], arr[j]] = [arr[j], arr[i]]
  }
  return arr
}
```

### 3.2 素材管理 Hook

**文件**：`src/hooks/useAssetManager.ts`

```typescript
import { useState, useCallback, useEffect } from 'react'
import { PuzzleAsset, AssetStatus } from '@/types/puzzle'

const STORAGE_KEY = 'puzzle-assets'

// 初始扫描 public/images/puzzle/ 获取素材列表
// 实际实现时需要根据项目结构调整
const INITIAL_ASSETS: PuzzleAsset[] = [
  // AI 开发时需要扫描实际文件夹填充
  { id: 'bat', src: '/images/puzzle/bat.jpg', status: 'fresh', lastUsedAt: 0 },
  { id: 'fish', src: '/images/puzzle/fish.jpg', status: 'fresh', lastUsedAt: 0 },
  { id: 'peony', src: '/images/puzzle/peony.jpg', status: 'fresh', lastUsedAt: 0 },
  // ... 更多素材
]

export function useAssetManager() {
  const [assets, setAssets] = useState<PuzzleAsset[]>(() => {
    if (typeof window === 'undefined') return INITIAL_ASSETS
    const stored = localStorage.getItem(STORAGE_KEY)
    return stored ? JSON.parse(stored) : INITIAL_ASSETS
  })
  
  const [currentAssetId, setCurrentAssetId] = useState<string | null>(null)
  
  // 持久化
  useEffect(() => {
    localStorage.setItem(STORAGE_KEY, JSON.stringify(assets))
  }, [assets])
  
  // 获取当前素材
  const currentAsset = assets.find(a => a.id === currentAssetId) || null
  
  // 选择新素材（换一换）
  const selectNewAsset = useCallback(() => {
    // 1. 标记当前为 skipped（如果有）
    setAssets(prev => {
      if (!currentAssetId) return prev
      return prev.map(a =>
        a.id === currentAssetId
          ? { ...a, status: 'skipped' as AssetStatus, lastUsedAt: Date.now() }
          : a
      )
    })
    
    // 2. 筛选 fresh 素材
    const freshAssets = assets.filter(a => a.status === 'fresh')
    
    // 3. 如果没有 fresh，重置所有为 fresh
    if (freshAssets.length === 0) {
      setAssets(prev => prev.map(a => ({ ...a, status: 'fresh' as AssetStatus })))
      // 递归调用（此时全部 fresh）
      setTimeout(() => selectNewAsset(), 0)
      return
    }
    
    // 4. 随机选择
    const randomAsset = freshAssets[Math.floor(Math.random() * freshAssets.length)]
    setCurrentAssetId(randomAsset.id)
  }, [assets, currentAssetId])
  
  // 标记当前为 played
  const markAsPlayed = useCallback(() => {
    if (!currentAssetId) return
    setAssets(prev =>
      prev.map(a =>
        a.id === currentAssetId
          ? { ...a, status: 'played' as AssetStatus, lastUsedAt: Date.now() }
          : a
      )
    )
  }, [currentAssetId])
  
  // 重置所有状态（用于测试）
  const resetAll = useCallback(() => {
    setAssets(INITIAL_ASSETS)
    setCurrentAssetId(null)
  }, [])
  
  return {
    assets,
    currentAsset,
    currentAssetId,
    selectNewAsset,
    markAsPlayed,
    resetAll
  }
}
```

### 3.3 游戏状态 Hook

**文件**：`src/hooks/usePuzzleGame.ts`

```typescript
import { useState, useCallback, useEffect } from 'react'
import { 
  GameState, PuzzlePiece, Difficulty, DropResult 
} from '@/types/puzzle'
import { sliceImage, shuffleArray } from '@/lib/imageUtils'

export function usePuzzleGame(assetSrc: string | null, difficulty: Difficulty) {
  const [state, setState] = useState<GameState>({
    assetId: null,
    difficulty,
    pieces: [],
    isCompleted: false,
    startTime: Date.now(),
    endTime: null
  })
  
  const [isLoading, setIsLoading] = useState(false)
  
  // 初始化游戏
  const initGame = useCallback(async () => {
    if (!assetSrc) return
    
    setIsLoading(true)
    
    try {
      // 1. 切片
      const imageUrls = await sliceImage(assetSrc, difficulty)
      
      // 2. 创建拼图块
      const pieces: PuzzlePiece[] = imageUrls.map((url, index) => ({
        id: index,
        correctRow: Math.floor(index / difficulty),
        correctCol: index % difficulty,
        currentRow: null,
        currentCol: null,
        imageUrl: url
      }))
      
      // 3. 打乱顺序（用于候选区显示）
      const shuffledPieces = shuffleArray(pieces)
      
      setState({
        assetId: assetSrc,
        difficulty,
        pieces: shuffledPieces,
        isCompleted: false,
        startTime: Date.now(),
        endTime: null
      })
    } finally {
      setIsLoading(false)
    }
  }, [assetSrc, difficulty])
  
  // 素材变化时重新初始化
  useEffect(() => {
    initGame()
  }, [initGame])
  
  // 放置拼图块
  const placePiece = useCallback((pieceId: number, row: number, col: number): DropResult => {
    const piece = state.pieces.find(p => p.id === pieceId)
    if (!piece) return { type: 'fail', reason: 'out-of-range' }
    
    // 检查目标格子
    const targetPiece = state.pieces.find(
      p => p.currentRow === row && p.currentCol === col
    )
    
    // 如果格子已有碎片
    if (targetPiece) {
      // 如果拖拽的是正确碎片，交换
      if (piece.correctRow === row && piece.correctCol === col) {
        setState(prev => ({
          ...prev,
          pieces: prev.pieces.map(p => {
            if (p.id === pieceId) return { ...p, currentRow: row, currentCol: col }
            if (p.id === targetPiece.id) return { ...p, currentRow: null, currentCol: null }
            return p
          })
        }))
        return { type: 'swap', row, col, targetPieceId: targetPiece.id }
      }
      // 否则失败
      return { type: 'fail', reason: 'occupied-wrong' }
    }
    
    // 检查是否在吸附范围内（30%）
    // 这个检查在拖拽释放时做，这里假设已经通过检查
    
    // 放置碎片
    setState(prev => ({
      ...prev,
      pieces: prev.pieces.map(p =>
        p.id === pieceId ? { ...p, currentRow: row, currentCol: col } : p
      )
    }))
    
    return { type: 'success', row, col }
  }, [state.pieces])
  
  // 移回候选区
  const returnToPool = useCallback((pieceId: number) => {
    setState(prev => ({
      ...prev,
      pieces: prev.pieces.map(p =>
        p.id === pieceId ? { ...p, currentRow: null, currentCol: null } : p
      )
    }))
  }, [])
  
  // 检查是否完成
  useEffect(() => {
    const allPlaced = state.pieces.every(
      p => p.currentRow === p.correctRow && p.currentCol === p.correctCol
    )
    
    if (allPlaced && !state.isCompleted && state.pieces.length > 0) {
      setState(prev => ({
        ...prev,
        isCompleted: true,
        endTime: Date.now()
      }))
    }
  }, [state.pieces, state.isCompleted])
  
  // 重置
  const reset = useCallback(() => {
    initGame()
  }, [initGame])
  
  // 切换难度
  const changeDifficulty = useCallback((newDifficulty: Difficulty) => {
    setState(prev => ({ ...prev, difficulty: newDifficulty }))
    // 重新初始化
    setTimeout(() => initGame(), 0)
  }, [initGame])
  
  return {
    state,
    isLoading,
    placePiece,
    returnToPool,
    reset,
    changeDifficulty
  }
}
```

### 3.4 拖拽实现（@dnd-kit）

**安装**：`npm install @dnd-kit/core @dnd-kit/utilities`

**文件**：`src/components/puzzle/PuzzlePiece.tsx`

```typescript
import { useDraggable } from '@dnd-kit/core'
import { CSS } from '@dnd-kit/utilities'
import { PuzzlePiece as PuzzlePieceType } from '@/types/puzzle'

interface Props {
  piece: PuzzlePieceType
  size: number
  isInPool?: boolean
}

export default function PuzzlePiece({ piece, size, isInPool = false }: Props) {
  const { attributes, listeners, setNodeRef, transform, isDragging } = useDraggable({
    id: `piece-${piece.id}`,
    data: { piece }
  })
  
  const style = {
    transform: CSS.Translate.toString(transform),
    width: size,
    height: size,
    backgroundImage: `url(${piece.imageUrl})`,
    backgroundSize: 'cover',
    borderRadius: '2px'
  }
  
  return (
    <div
      ref={setNodeRef}
      {...listeners}
      {...attributes}
      style={style}
      className={`
        cursor-grab active:cursor-grabbing
        transition-shadow
        ${isDragging ? 'shadow-2xl scale-110 opacity-80 z-50' : 'shadow-md'}
        ${isInPool ? '' : 'ring-2 ring-white/50'}
      `}
    />
  )
}
```

**文件**：`src/components/puzzle/PuzzleBoard.tsx`

```typescript
import { useDroppable } from '@dnd-kit/core'
import { Difficulty, PuzzlePiece } from '@/types/puzzle'

interface Props {
  difficulty: Difficulty
  pieces: PuzzlePiece[]
  boardSize: number
  onPieceDrop: (pieceId: number, row: number, col: number) => void
}

export default function PuzzleBoard({ difficulty, pieces, boardSize, onPieceDrop }: Props) {
  const cellSize = boardSize / difficulty
  
  // 生成网格
  const cells = []
  for (let row = 0; row < difficulty; row++) {
    for (let col = 0; col < difficulty; col++) {
      const piece = pieces.find(
        p => p.currentRow === row && p.currentCol === col
      )
      
      cells.push(
        <DroppableCell
          key={`${row}-${col}`}
          row={row}
          col={col}
          size={cellSize}
          piece={piece}
        />
      )
    }
  }
  
  return (
    <div 
      className="grid border-4 border-paper-500 bg-white"
      style={{
        gridTemplateColumns: `repeat(${difficulty}, 1fr)`,
        width: boardSize,
        height: boardSize
      }}
    >
      {cells}
    </div>
  )
}

// 单个可放置格子
function DroppableCell({ row, col, size, piece }: {
  row: number
  col: number
  size: number
  piece?: PuzzlePiece
}) {
  const { isOver, setNodeRef } = useDroppable({
    id: `cell-${row}-${col}`,
    data: { row, col }
  })
  
  return (
    <div
      ref={setNodeRef}
      className={`
        border border-rice-300
        flex items-center justify-center
        ${isOver ? 'bg-paper-100' : ''}
      `}
      style={{ width: size, height: size }}
    >
      {piece && (
        <div
          style={{
            width: size,
            height: size,
            backgroundImage: `url(${piece.imageUrl})`,
            backgroundSize: 'cover'
          }}
          className="rounded-sm"
        />
      )}
    </div>
  )
}
```

**文件**：`src/components/puzzle/PuzzleGame.tsx`（拖拽上下文）

```typescript
import { DndContext, DragEndEvent, DragOverlay, DragStartEvent } from '@dnd-kit/core'
import { useState } from 'react'
import { usePuzzleGame } from '@/hooks/usePuzzleGame'
import { useAssetManager } from '@/hooks/useAssetManager'
import PuzzleBoard from './PuzzleBoard'
import PiecePool from './PiecePool'
import PuzzlePiece from './PuzzlePiece'
import { Difficulty } from '@/types/puzzle'

export default function PuzzleGame() {
  const { currentAsset, selectNewAsset, markAsPlayed } = useAssetManager()
  const [difficulty, setDifficulty] = useState<Difficulty>(3)
  
  const { state, isLoading, placePiece, returnToPool, reset, changeDifficulty } = 
    usePuzzleGame(currentAsset?.src || null, difficulty)
  
  const [activePiece, setActivePiece] = useState<any>(null)
  
  // 拖拽开始
  const handleDragStart = (event: DragStartEvent) => {
    setActivePiece(event.active.data.current?.piece)
  }
  
  // 拖拽结束
  const handleDragEnd = (event: DragEndEvent) => {
    const { active, over } = event
    
    if (!over) {
      // 没有放置到有效区域，飞回候选区
      const pieceId = active.data.current?.piece?.id
      if (pieceId !== undefined) returnToPool(pieceId)
      setActivePiece(null)
      return
    }
    
    const pieceId = active.data.current?.piece?.id
    const dropData = over.data.current
    
    if (dropData?.row !== undefined && dropData?.col !== undefined) {
      // 放置到拼图板
      const result = placePiece(pieceId, dropData.row, dropData.col)
      
      if (result.type === 'fail') {
        // 失败，飞回
        returnToPool(pieceId)
      }
    }
    
    setActivePiece(null)
  }
  
  // 计算吸附阈值（格子尺寸的 30%）
  const boardSize = typeof window !== 'undefined' && window.innerWidth < 768 ? 300 : 400
  const cellSize = boardSize / difficulty
  const snapThreshold = cellSize * 0.3
  
  if (isLoading) return <div>加载中...</div>
  
  return (
    <DndContext
      onDragStart={handleDragStart}
      onDragEnd={handleDragEnd}
      // 配置吸附阈值
      collisionDetection={
        // 使用 closestCenter 或其他策略
      }
    >
      <div className="min-h-screen bg-rice-50 p-4">
        {/* 顶栏 */}
        <header className="flex items-center justify-between mb-6">
          <button onClick={onBack} className="text-ink-700">← 返回</button>
          <h1 className="text-xl font-serif font-bold">纹样拼图</h1>
          <div className="flex items-center gap-3">
            <select 
              value={difficulty}
              onChange={(e) => changeDifficulty(Number(e.target.value) as Difficulty)}
              className="border rounded px-2 py-1"
            >
              <option value={3}>入门 3×3</option>
              <option value={4}>进阶 4×4</option>
            </select>
            <button 
              onClick={selectNewAsset}
              className="flex items-center gap-1 px-3 py-1 bg-paper-500 text-white rounded"
            >
              <span>🔄</span>
              <span>换一换</span>
            </button>
          </div>
        </header>
        
        {/* 主内容 */}
        <div className="flex flex-col md:flex-row gap-6 items-start justify-center">
          {/* 左侧：预览/完成信息 */}
          <div className="w-full md:w-48">
            {state.isCompleted ? (
              <div className="text-center">
                <img 
                  src={currentAsset?.src} 
                  alt="完成"
                  className="w-full rounded shadow-lg"
                />
                <p className="mt-2 text-green-600 font-bold">完成！</p>
                <p className="text-sm text-gray-500">
                  用时 {Math.floor((state.endTime! - state.startTime) / 1000)} 秒
                </p>
              </div>
            ) : (
              <div className="bg-ink-900 rounded p-4 text-center">
                <div className="text-4xl mb-2">?</div>
                <p className="text-white/60 text-sm">拼完揭晓</p>
              </div>
            )}
          </div>
          
          {/* 右侧：拼图板 */}
          <PuzzleBoard
            difficulty={difficulty}
            pieces={state.pieces}
            boardSize={boardSize}
          />
        </div>
        
        {/* 候选区 */}
        <PiecePool 
          pieces={state.pieces.filter(p => p.currentRow === null)}
          pieceSize={cellSize}
        />
        
        {/* 拖拽时的浮动预览 */}
        <DragOverlay>
          {activePiece ? (
            <PuzzlePiece 
              piece={activePiece} 
              size={cellSize}
              isInPool={false}
            />
          ) : null}
        </DragOverlay>
        
        {/* 完成弹层 */}
        {state.isCompleted && (
          <CompletionModal 
            onNext={selectNewAsset}
            onReplay={reset}
          />
        )}
      </div>
    </DndContext>
  )
}
```

### 3.5 吸附判定实现

**文件**：`src/lib/dragUtils.ts`

```typescript
import { CollisionDetection, ClientRect } from '@dnd-kit/core'

/**
 * 自定义碰撞检测：距离中心点 30% 以内才算碰撞
 */
export function createSnapCollision(threshold: number): CollisionDetection {
  return (args) => {
    const { droppableContainers, pointerCoordinates } = args
    
    if (!pointerCoordinates) return []
    
    const collisions = []
    
    for (const container of droppableContainers) {
      const rect = container.rect.current
      if (!rect) continue
      
      const centerX = rect.left + rect.width / 2
      const centerY = rect.top + rect.height / 2
      
      const distance = Math.sqrt(
        Math.pow(pointerCoordinates.x - centerX, 2) +
        Math.pow(pointerCoordinates.y - centerY, 2)
      )
      
      // 距离在阈值内才算碰撞
      if (distance <= threshold) {
        collisions.push({
          id: container.id,
          data: { distance }
        })
      }
    }
    
    // 按距离排序，返回最近的
    return collisions.sort((a, b) => a.data.distance - b.data.distance)
  }
}
```

### 3.6 飞回动画实现

**文件**：`src/components/puzzle/PiecePool.tsx`

```typescript
import { useDroppable } from '@dnd-kit/core'
import { PuzzlePiece } from '@/types/puzzle'
import PuzzlePieceComponent from './PuzzlePiece'

interface Props {
  pieces: PuzzlePiece[]
  pieceSize: number
}

export default function PiecePool({ pieces, pieceSize }: Props) {
  const { setNodeRef, isOver } = useDroppable({
    id: 'pool',
    data: { type: 'pool' }
  })
  
  return (
    <div
      ref={setNodeRef}
      className={`
        mt-8 p-4 bg-white rounded-lg shadow-inner
        ${isOver ? 'bg-paper-50' : ''}
      `}
    >
      <p className="text-center text-gray-500 mb-4 text-sm">候选区</p>
      
      <div 
        className="flex flex-wrap justify-center gap-2"
        style={{ minHeight: pieceSize + 20 }}
      >
        {pieces.map(piece => (
          <div
            key={piece.id}
            className="animate-fly-in"  // 飞入动画
          >
            <PuzzlePieceComponent
              piece={piece}
              size={pieceSize}
              isInPool={true}
            />
          </div>
        ))}
      </div>
    </div>
  )
}
```

**CSS 动画**（`globals.css`）：

```css
/* 飞回候选区的动画 */
@keyframes flyIn {
  0% {
    opacity: 0;
    transform: scale(0.8) translateY(-20px);
  }
  100% {
    opacity: 1;
    transform: scale(1) translateY(0);
  }
}

.animate-fly-in {
  animation: flyIn 0.3s ease-out;
}

/* 放置成功的动画 */
@keyframes snap {
  0% { transform: scale(1.1); }
  50% { transform: scale(0.95); }
  100% { transform: scale(1); }
}

.animate-snap {
  animation: snap 0.2s ease-out;
}

/* 错误提示闪烁 */
@keyframes flashRed {
  0%, 100% { background-color: transparent; }
  50% { background-color: rgba(220, 38, 38, 0.2); }
}

.animate-flash-red {
  animation: flashRed 0.3s ease-in-out 2;
}
```

### 3.7 完成庆祝效果

**文件**：`src/components/puzzle/PaperConfetti.tsx`

```typescript
import { useEffect, useState } from 'react'

interface Props {
  active: boolean
}

export default function PaperConfetti({ active }: Props) {
  const [particles, setParticles] = useState<Array<{
    id: number
    x: number
    delay: number
    duration: number
    rotation: number
  }>>([])
  
  useEffect(() => {
    if (!active) return
    
    // 生成 30 个纸屑粒子
    const newParticles = Array.from({ length: 30 }, (_, i) => ({
      id: i,
      x: Math.random() * 100,  // 随机水平位置
      delay: Math.random() * 0.5,  // 随机延迟
      duration: 1.5 + Math.random(),  // 随机下落时间
      rotation: Math.random() * 360  // 随机旋转
    }))
    
    setParticles(newParticles)
    
    // 2秒后清理
    const timer = setTimeout(() => setParticles([]), 3000)
    return () => clearTimeout(timer)
  }, [active])
  
  if (!active || particles.length === 0) return null
  
  return (
    <div className="fixed inset-0 pointer-events-none overflow-hidden z-50">
      {particles.map(p => (
        <div
          key={p.id}
          className="absolute w-2 h-2 bg-paper-500"
          style={{
            left: `${p.x}%`,
            top: '-10px',
            clipPath: 'polygon(50% 0%, 0% 100%, 100% 100%)',
            animation: `confetti-fall ${p.duration}s linear ${p.delay}s forwards`,
            transform: `rotate(${p.rotation}deg)`
          }}
        />
      ))}
    </div>
  )
}
```

**CSS**：

```css
@keyframes confetti-fall {
  0% {
    transform: translateY(0) rotate(0deg);
    opacity: 1;
  }
  100% {
    transform: translateY(100vh) rotate(720deg);
    opacity: 0;
  }
}
```

---

## 四、与网站整合

**文件**：`src/components/sections/InteractiveSection.tsx`

```typescript
'use client'

import { useState } from 'react'
import PuzzleGame from '@/components/puzzle/PuzzleGame'

export default function InteractiveSection() {
  const [showGame, setShowGame] = useState(false)
  
  if (showGame) {
    return (
      <div className="fixed inset-0 z-50 bg-rice-50">
        <PuzzleGame onBack={() => setShowGame(false)} />
      </div>
    )
  }
  
  return (
    <section className="bg-rice-50 py-16 md:py-24 px-6">
      <div className="max-w-4xl mx-auto text-center">
        <h2 className="text-3xl md:text-4xl font-serif font-bold text-ink-900 mb-4">
          纹样拼图
        </h2>
        <p className="text-ink-600 mb-8">还原破碎的剪纸之美</p>
        
        <div 
          onClick={() => setShowGame(true)}
          className="inline-block bg-paper-500 text-white px-8 py-4 rounded-sm
                     cursor-pointer hover:bg-paper-600 transition-all
                     hover:shadow-lg hover:-translate-y-1"
        >
          开始挑战
        </div>
      </div>
    </section>
  )
}
```

---

## 五、开发执行顺序

```
Step 1:  创建类型定义（types/puzzle.ts）
Step 2:  实现图片切片工具（lib/imageUtils.ts）
Step 3:  实现素材管理 Hook（hooks/useAssetManager.ts）
Step 4:  实现游戏状态 Hook（hooks/usePuzzleGame.ts）
Step 5:  安装 @dnd-kit 依赖
Step 6:  实现 PuzzlePiece 组件（可拖拽）
Step 7:  实现 PuzzleBoard 组件（可放置网格）
Step 8:  实现 PiecePool 组件（候选区）
Step 9:  实现吸附判定逻辑（lib/dragUtils.ts）
Step 10: 实现飞回动画
Step 11: 组装 PuzzleGame 主组件
Step 12: 实现完成检测和庆祝效果
Step 13: 实现 AssetPreview 和 CompletionModal
Step 14: 整合到 InteractiveSection
Step 15: 添加 CSS 动画
Step 16: 移动端适配测试
Step 17: 准备拼图素材到 public/images/puzzle/
Step 18: 完整流程测试
```

---

## 六、关键注意事项

### 6.1 图片跨域问题

如果素材图片跨域，Canvas 会污染。解决方案：
- 确保图片和网站同域
- 或配置图片服务器允许 CORS
- 或在 `next.config.js` 中配置图片域名

### 6.2 内存管理

Blob URL 需要释放：
```typescript
useEffect(() => {
  return () => {
    // 组件卸载时释放 Blob URL
    pieces.forEach(p => URL.revokeObjectURL(p.imageUrl))
  }
}, [])
```

### 6.3 移动端触摸优化

@dnd-kit 默认支持触摸，但需要测试：
- 触摸时碎片不能太小（建议最小 60px）
- 触摸时手指不能遮挡碎片
- 考虑添加触摸反馈（震动 API）

### 6.4 素材文件夹结构

```
public/
└── images/
    └── puzzle/
        ├── bat.jpg
        ├── fish.jpg
        ├── peony.jpg
        ├── dragon.jpg
        ├── rabbit.jpg
        └── ... (至少 10 张)
```

---

## 七、测试清单

- [ ] 图片正确切片为正方形
- [ ] 3×3 和 4×4 难度切换正常
- [ ] 拖拽流畅，无卡顿
- [ ] 吸附范围正确（30%）
- [ ] 错误时飞回动画正常
- [ ] 换一换不重复出现已用素材
- [ ] 全部素材用完后可循环
- [ ] 完成检测准确
- [ ] 庆祝动画正常
- [ ] 移动端触摸正常
- [ ] 内存无泄漏
